# 全日交通量及車種組成 v20.40 驗證報告

- 驗證日期：2026-09-01
- 發布目標：`BaoToast/DailyTraffic` 的 GitHub Pages
- 前一正式版：v20.39（commit `378158a85c8e8480a145b5005a09c6ee1bca283d`）

## 本版性質

v20.40 修正季度輸入與備份還原的資料識別邊界，以及平假日比較圖表的缺值快取。**交通量、PCU、尖峰、車種組成與方向分組的計算公式均未變更。**

11 路段黃金值仍為全日實際交通量 `688,205 輛／日`、24 小時 PCU `530,122 PCU／日`。

## Claude 原交付修正

1. 新增 `checkSurveyPeriodInput()` 與對應訊息，避免超出換算窗口的四碼西元年份原樣存入。
2. 民國兩碼年份（例如 `99Q4`）可正常匯入及從備份還原。
3. 平假日比較圖表的內嵌快取對「本季未調查」寫入 `null`，不再畫成 0。

## GPT 複查追加修正

### 1. 民國年範圍檢查仍會放行超範圍年份

原交付版只檢查正規化後是否長得像二～三碼季度，會放行 `89Q1`、`201Q3`、`999Q1`。這與介面說明的允許範圍「民國 90～200／西元 2001～2111」不一致。

現改為先正規化全半形與空白，再依原始年份位數明確判定：

- 二～三碼視為民國年，只允許 90～200。
- 四碼視為西元年，只允許 2001～2111。
- 格式錯誤與年份超範圍分別提供訊息。

### 2. 備份還原可能無聲遺失或留下非標準季度

原交付版以 `filter()` 移除不合格季度；一份備份同時含有效與無效資料時，只會還原其中一部分，最後仍可能回報成功。合法的西元季度雖通過檢查，寫回時也仍保留原字串，違反「一律以民國年儲存」的界線。

現改為：

- 任一筆季度不合格即停止還原，指出第幾筆及原因，不做部分還原。
- 合法的西元季度在寫回前轉成民國年鍵。
- 若現有資料以另一種年份寫法保存同一季，明確阻擋並說明，不建立重複季度。

### 3. 驗證報告內容仍是 v20.39

原交付檔名為 `VALIDATION_v20.40.md`，但標題、前一正式版、驗證數量與結論仍是 v20.39。現已改為本報告，並擴充發布中繼資料守門測試：不只檢查檔名，也檢查報告標題與前一正式版 commit。

## 驗證結果

- TypeScript `tsc --noEmit`：0 錯誤。
- ESLint：0 錯誤；7 項既有 React Hook 建議警告，與 v20.39 相同。
- 單元／回歸測試：**269/269 通過**。
- 端對端：`npm run e2e` **154 項檢查、0 失敗**，瀏覽器主控台無 JavaScript 例外。
- 計算黃金值：11 路段基準與 PCU 係數黃金值全部一致（24/24 通過）。
- 發布／備份／版本守門：季度範圍、備份完整還原、圖表缺值、版號、手冊與根目錄建置資產均通過。

## 真實檔驗算的證據邊界

Claude 隨交付說明表示，已使用 37 份原始工作簿逐張重跑，2,256 列明細與 v20.38 逐字相同，並完成兩支真實檔測試。本次 GPT 複查沒有收到該 37 份工作簿，因此不把這項敘述當成本機重新執行的結果；本次可重現的證據是匿名樣本、單元測試、完整 E2E、黃金值與正式發布資產雜湊。

## 手冊檢查

- Word 與 PDF 均為 v20.40，下載連結與版號一致。
- 已逐頁渲染檢查，未見文字截斷、重疊、缺字或表格破版。

## 已知套件警示

`npm ci` 仍回報 23 項相依套件警示（1 low、6 moderate、16 high）。依既定維護規則未強制升級，避免在未完成舊版 Excel 匯入／匯出相容性評估前改動套件。

## 結論

v20.40 的季度輸入、備份還原與圖表缺值問題均已修正並補上守門測試；既有計算口徑與黃金值未變，可發布至 GitHub Pages。
