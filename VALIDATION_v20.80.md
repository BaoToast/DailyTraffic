# 全日交通量及車種組成 v20.80 驗證報告

## v20.80（2026-09-19）獨立複查重點

- **風險等級：高**。這次正式候選是自可驗證的 v20.63 累積到 v20.80，且包含解析、
  PCU 範圍、跨季度趨勢、Excel 與 UI 資料流變更；依正式規則採全專案複查。
- **使用者最新裁示**：歷季「單一車種佔比」與「大車比例」須逐調查點各自計算。
  每一點均以自己的分子總和除以分母總和；不跨點合併，也不平均各點百分比。
- **Excel 同步修正**：「歷季組成圖表資料」改為季度 × 調查點 × 日別逐列；
  平日＋假日並列、不相加，原生堆疊圖同步改讀新欄位。
- **不變範圍**：「歷季全日交通量」「歷季車種組成」兩張明細底稿算法不變；
  車種組成頁、目前車種組成 Excel 與草稿的跨點合計不加回。
- **反證**：先只更新守門、仍使用 v20.79 舊邏輯時，歷季大車比例 3 項紅；
  Excel 組成圖表資料 2 項紅。修正後兩支針對測試均全綠。
- **GPT 複查中修正**：Windows 測試路徑改用 `fileURLToPath`；手冊的計畫清單位置改為
  與現行 UI 一致；E2E 樣本每輪先清空以避免中斷後殘留資料污染下一輪；
  `e2e-text-wrap.mjs` 移除寫死的 Linux Chromium 路徑，改用專案既有的跨平台啟動器；
  移除已取消 DOCX 交付後殘留、且引用未宣告 `docx` 套件的舊建置腳本；
  試用版建置會先建立輸出資料夾，搬遷到新的正式工作區後不再因上層 `out` 尚不存在而失敗。
- **真實公司附件限制**：本次交付未包含 Claude 所述的五份真實路段檔與五份路口檔，
  因此該項不能算作 GPT 獨立重跑通過；以程式內樣本、黃金值與完整自動測試取代驗證。
- **完整自動驗證**：`npm test` 共 536 項：535 通過、0 失敗、1 跳過；跳過項目是
  未提供真實公司檔名附件時的條件式測試。lint、TypeScript、字形守門與 production build
  均通過。`npm run e2e` 的 56 支可執行瀏覽器腳本以串行方式完整跑完，56/56 通過。
- **E2E 數量更正**：Repository 共有 58 個 `scripts/e2e-*.mjs` 檔；其中
  `e2e-nav.mjs` 是共用 helper、不是測試，`e2e-tryout-smoke.mjs` 由獨立的
  `npm run e2e:tryout` 執行。Claude 文件把 58 個檔案寫成 58 支正式 E2E 並不精確；
  本次實際結果是正式套件 56/56，加上試用版 smoke 1/1。已新增 inventory 守門，
  以後可執行的 E2E 若沒有掛進正式 npm 入口會直接失敗。
- **正式工作區重驗**：上述 `npm test` 與 56 支串行 E2E 均在
  `D:\Users\95108\Documents\Codex\DailyTraffic` 完成；`npm run build:tryout`、
  試用版 `file://` 核心流程 smoke test 及 `npm run build:pages` 亦全部通過。
- **黃金值**：`tests/traffic-parser.test.mjs` 仍驗得 688,205 輛／日、530,122 PCU／日，
  本次比例呈現修正沒有改動既有交通量或 PCU 算法。
- **指定版面與 PNG**：1536×864、1366×768 各自逐頁檢查全部 14 頁；未見文字重疊、
  橫向溢出、按鈕裁切或圖表越界。歷季多調查點的線尾沒有名稱標籤，圖例位於圖下方且
  不與 X 軸名稱重疊；下載 PNG 結果相同。平假日長條圖 PNG 沒有靜態數值標籤，符合設計。
- **真正 Microsoft Excel 驗證**：以本機 Microsoft Excel 16.0 唯讀開啟 20 張工作表；
  「目前車種組成」日別只有平日／假日且沒有合計；「假日相較平日（%）」為 `0.0%`
  百分比格式；7 張可編輯原生圖表皆由 Excel 正常載入，具有有效尺寸、標題與資料序列。
- **手冊**：兩個正式位置的 v20.80 PDF SHA-256 一致；30 頁全部轉圖檢視，未見裁切、
  重疊、表格溢出或異常空白破頁。
- **依賴稽核**：production 為 0 high／0 critical（2 moderate）；全依賴為
  1 low／8 moderate／16 high，均位於開發／建置工具鏈，列為後續 dependency 維護，
  不在本次功能修正中擴大升級。
- **發布證據**：正式程式 release commit
  `da4d6505cff50d14c46572673dd78eb0bd8a9d00` 已推送到 `BaoToast/DailyTraffic` `main`；
  GitHub Actions `建置與測試` run 35476224593 與 `pages build and deployment` run
  35476224285 均成功。線上首頁引用 v20.80 新資產，主 JS 線上／本機 SHA-256 同為
  `75C7AC942C80FCB62191BA361E9154B877F52B4F654B01B7F165C01855BFF001`；
  v20.80 手冊與本報告 HTTP 200，v20.63 舊 JS／CSS／PDF／DOCX 專屬 URL 均為 404。

---

## v20.79（2026-09-18）本版驗證重點

- **沒有變更任何計算算法**：黃金值 688,205 輛／日、530,122 PCU／日不變；
  五份真實路段檔與五份路口檔以 Python 獨立重算（總量、PCU、尖峰小時、車種占比）與畫面逐格相同。
- **反向守門**：e2e-filter-coverage 新增「寫著不適用的塊，數字不可以跟著變」；
  在 v20.78 建置上 3 項紅（歷季分析日別×2、可追溯明細時段），本版全綠。
- **車種組成的時段**：上午／下午尖峰改為「整個調查點同一時段」那一小時（原本是半天所有小時）；
  並列時兩組圓環；單位逐點依涵蓋。守門：e2e-audit-split、e2e-filter-coverage。
- **合計行全數移除**（使用者 09-18 裁示）：跨調查點合計比例、兩天合計；Excel 目前車種組成改一點×一日別。
  反證：e2e-audit-split 在 v20.78 上紅（那一行仍在）。
- **歷季分析多線**：不設上限、顏色循環＋線型、線尾不標名稱；e2e-trend-end-labels 在 v20.78 上 2 紅
  （線尾 137px 墨、彩色 0 種）。
- **匯出**：平假日比較百分比欄位、季別欄、浮點雜訊；24 小時型態 PNG 逐調查點；比較圖 PNG 淨空＋留白。
- **文字**：Markdown 星號 0 處（tests/plaintext-markup 擴到 app/ 全部）；位置代稱 0 處（tests/wording）。
- **跨系統**：T15-01 七叉路口與路口轉向在相同轉向設定與係數下逐格一致。
- 當時交付文件記為「e2e 58 支全綠」；本次盤點確認該數字混入 1 支 helper 與
  1 支獨立試用版 smoke，正式 `npm run e2e` 實為 56 支串行腳本。

---

## 前一版（v20.78）驗證紀錄

## v20.78（2026-09-17）本版驗證重點

- **加總／並列稽核的最後五項**（C、E、F、G、①）。9/16 的稽核清單逐項回頭查證，
  A、B、D、② 早就修好，這五項還沒動，本版一次補完。
- **C 反證實測**：把季別分組鍵拿掉之後，兩季 42,090 被加成 **84,180**，
  而畫面上沒有任何地方看得出來——這正是「靜默」的意思。
- **「已人工確認」鈕**（使用者 9/17 指名名稱）：三個踩過的雷各一條守門，
  每一條都做過反證（塞窄→②紅、調淡→③紅、加大 padding→④紅）。
- 守門：`scripts/e2e-audit-split.mjs`（C 與 ①，含兩條對照組）、
  `tests/chart-notes.test.mjs`（E、F，含兩條對照組）、
  `tests/cross-system-guards.test.mjs`（G）、`tests/issue-ack.test.mjs`（名稱）。
- 單元測試 529＋13 條全綠。

## v20.76（2026-09-17）本版驗證重點

- **匯入時「重複」與「欄序和標題對不上」都要出聲**（X-66）。使用者問三支程式
  是否都認標籤、欄位換位讀不讀得到、重複會不會指出異常，查出來本支有兩個
  完全安靜的洞（詳見第六節）。
- 守門：`tests/duplicate-and-swapped-columns.test.mjs`（5 條，其中 2 條是
  「正常檔案不可以叫」的對照組），反證實跑過。
- 單元測試 526＋13 條全綠。

## v20.75（2026-09-17）本版驗證重點

- **側欄改成三層、14 個大分頁**（X-63）：`e2e-tabs` 從「逐分區」改成「逐大分頁」，
  每一頁除了驗自己的招牌內容，還多一條**以區塊 id 比對**的洩漏檢查
  （別頁那幾塊的 id 出現在 DOM 裡就紅）。
- 「刪除單一季度」搬到「還原與備份」；`maintenance-page.test.mjs` 的
  「每一塊都要掛常駐說明」改成**逐塊**驗（比原本數總數更嚴）。
- 全套 e2e（53 支）與單元測試（521＋13 條）全綠。
- ⚠️ 換掉一支假守門：`e2e-sticky-offset` 的「兩種狀態間距相同」在拆頁之後
  量不到黏住的位置，已改成直接量 `top` 是不是「主工具列高＋留白」。


## v20.75（2026-09-14）：主工具列上線（三態：鏡子／脫離／回歸）

**不含任何計算口徑變更。** 升級前後以同一份測資逐頁逐格比對畫面上的每一個數字：
5 頁、738 個數字，**沒有任何既有數字消失或改變**（`scripts/capture-baseline.mjs`
存基準、`scripts/compare-baseline.mjs` 比對；除了 DOM 位置比對之外，另外做一次
「只比值、不比位置」的集合比對，才分得出「版面換了」與「數字變了」）。

### 使用者原話

> 「主工具列＝彙整所有圖表工具列的篩選條件＋路口流量視角＋尖峰時段判定」
> 「圖可以自己改，但只影響那一張」
> 「針對共同的篩選條件……圖表自身的工具列仍舊要與主工具列同步」
> 「主工具列也多一個全部回歸鈕……避免有部分圖表忘記點選回歸」
> 「有圖表不適用的條件，該圖表就單獨顯示不適用」
> 「季度一律只做起訖區間，預設起＝迄」
> 「確保圖表在主工具列每種篩選條件下，能計算或繪製的話，都要確實去計算或繪製圖形，
>   不要有遺漏。這方面我無法做檢查，只能靠你謹慎對待。」

### 這一版做了什麼

**主工具列**（每一頁都看得到）：季度起訖區間（預設起＝迄＝最新一季）、日別、
路段／路口多選、車流方向多選、調查時段（含「上午＋下午並列」）、
路口流量視角（含「駛出＋駛入並列」）、尖峰時段認定、顯示數值（含兩個組合選項）。

**三態**：
- 鏡子——沒動過的區塊顯示的就是主工具列的值；主工具列一改，那一顆看得到跟著變，
  而且數字真的重算。
- 脫離——在區塊上改條件，只有那一塊改用自己的值，並寫明
  「目前用本區塊自己的條件（主工具列：…）」。
- 回歸——每一塊脫離的區塊一顆「回到主工具列條件」；主工具列上另有一顆
  「回歸全部（N 塊）」。

**不適用的條件**單獨顯示不適用並寫出理由，而且**只在真的篩了那個條件時**才出現。
「並列」一律是呈現方式、不是篩選，資料層一筆都不會被篩掉。

**結論草稿與報表輸出維持獨立**，另加一顆「套用主工具列目前的條件」。

### 升級過程中抓到並修好的既有錯誤

1. **季度（起）預設值會把兩季加在一起。**
   基準比對量到某些格變成 1.667 倍（＝(100+150)/150）——原因是「季度（起）」
   預設落在最早一季，於是預設狀態下的區間涵蓋了兩季。已改成起＝迄＝最新一季
   （`quarterFromTouched`：使用者主動拉開之後才不再自動跟隨）。
   交通服務水準有同一個毛病，一併修。
2. **車種組成的說明把「五種車」寫成「大型車與特種車」**（逐頁肉眼檢查抓到）。
   畫面寫「大型車與特種車合計 18,375 輛（42.7%）」，但那兩項其實只有 10,239 輛；
   18,375 是所有非機車、非小型車的五種。數字對、句子錯，而使用者會照抄進報告。
   已改成把實際加總進去的車種逐一寫出來，並補兩條守門（反面測試已做）。
3. **時段車種分析的「顯示數值」選了沒反應。**
   那一顆寫進本區塊的條件，表格卻讀主工具列的值。已改成同一份。
   同一區的「尖峰時段認定」說明字、匯出中心的「跟著畫面走」、結論草稿一併改。

### 守門

- `app/main-filters.ts` ＋ `tests/main-filters.test.mjs`：條件模型（含「起＝迄不算篩」）。
- `scripts/e2e-main-toolbar.mjs`：三態，**連數字一起量**（只比下拉的 value 會被
  「value 對了但畫面沒重算」騙過去）。
- `scripts/e2e-filter-coverage.mjs`：每一個條件 × 每一區——不是真的算了，就是寫明不適用；
  反面守門：回到預設之後不可以還留著任何一句「不適用」。
- `scripts/e2e-apply-main.mjs`：結論草稿與報表**維持獨立**，而且按鈕按下去真的套得上。
- `scripts/probe-filter-matrix.mjs`：條件 × 區塊的變／註／✗ 矩陣（拿來找遺漏，不是守門）。

### 已知、尚未處理（需使用者裁示）

「大型車比例」在本程式有兩種口徑：車種組成的說明算所有非機車非小型車（42.7%），
歷季趨勢的指標 `heavyShare` 只算內建的大型車＋特種車（同一批資料是 23.8%）。
自訂車種（大客車、大貨車、聯結車）沒有在「車種分類與當量管理」歸類的話不會被後者算到。
**這是計算口徑的決定，不由程式自己改**；目前先各自把口徑寫清楚。

---

前一正式版：v20.63（commit `f77328c42131452bccdd3ef5da125da5ab8b9bf8`）

⚠️ 正式 GitHub Repository 在本次複查開始時的 `main` 為文件更新 commit
`c75385e9f516402d7e82f32def32b552b9113a3f`；其中正式程式仍是上述 v20.63。
Claude 交付資料所寫的 v20.65 commit `77cfa1faee68596eed5b368550255cc3fc6a0865`
不在目前 GitHub `main`、遠端 refs 或本機正式 Git history 中，GitHub commit URL 亦無法取得，
因此不能當作已發布的正式基準。本次須以可驗證的 v20.63→v20.80 進行全專案複查。


---

## 六、匯入判讀：重複與欄序（X-66，2026-09-17）

### 使用者原話

> 「另外兩支程式有這類問題嗎?三支程式是否都是讀取標籤，然後欄位如果有換位，
>   都能讀到，如果有重複，也能指出異常讓使用者去確認的功能嗎?」

### 查證結果：這一支是**混合**，而且兩個洞都是靜默的

認標籤文字的部分：表頭在第幾列、哪些欄是資料欄、車種名稱、時段欄、日別、
調查日期、「路口編號：」、目的支線（往B／往C）——全部掃文字，不看固定欄號。

認位置的部分（風險集中區）：

1. **方向**。全檔沒有任何一行讀「方向」「北向」這種表頭文字（`traffic-parser.ts`
   反而把「方向」列進排除清單）。方向由欄群的**順序**決定，名稱來自計畫設定。
2. **欄群怎麼切**。有「路口編號：」標記時照標記切（權威）；沒有時用
   「車種標題重複的週期」猜——**路段格式的調查表通常沒有那個標記**。
3. **左轉／直進／右轉**。純相對位移，`左轉`／`右轉` 這幾個字只被用來判斷
   「這是不是路口表」，從來沒有被用來決定哪一欄是左轉。

### 實測到的兩個事故（不是推測）

| 情境 | 實測結果 |
|---|---|
| 車種順序**整表一致地**調換 | ✅ 正確（分類鍵來自欄名） |
| **只有其中一個方向**調換 | ❌ 週期找不到 → 8 欄被當成同一個方向 → 北向 1,2,3,4／南向 10,20,30,40 變成**一筆** 11,22,33,44，南向整筆消失，匯入照樣成功 |
| 子標題寫 `右轉\|直進\|左轉` | ❌ 右轉 1 輛被記成左轉 1 輛，`sourceWarnings` 是空的 |
| 同一欄群裡標籤重複（相鄰） | 被當成合併儲存格**相加**，被蓋掉的那一個車種直接消失 |
| 同一欄群裡標籤重複（不相鄰） | 車輛數**累加**、轉向明細**後者蓋前者** → 同一筆紀錄總量與轉向明細互相矛盾（實測差 6 輛），沒有任何地方比對這兩者 |

下游的檢核全部抓不到：總量守恆、車種標籤集合相同（比的是**跨支線**的集合，
同一支線內重複去重後仍相同）、重複鍵值（比的是**資料列鍵**）、24 小時涵蓋。

### 處置

- 兩條表頭層級的檢查，結果掛進 `sourceWarnings`（已有的管道，匯入畫面會逐句列出）：
  - 一個方向裡同一個車種鍵出現在兩個 run → 說出是哪個車種，並**明講可能是
    方向切不出來**（只說「表頭打錯字」會把人帶錯方向）。
  - 轉向子標題讀得到、但順序與 左→直→右 對不上 → 說出第幾欄寫的是什麼。
- **不自動改讀法**。依欄名重排等於系統替調查資料做決定，而且會把合併儲存格
  那些版型改壞。只出聲，請使用者核對。

### 守門與反證

`tests/duplicate-and-swapped-columns.test.mjs`（5 條）。

⚠️ 兩條對照組是刻意的：只驗「壞檔案會叫」的話，一個「一律叫」的實作也會全綠，
而那會讓使用者學會忽略所有警告——比不叫更糟。對照組之一**故意用合併儲存格版型**
（車種名只寫在三欄的第一欄），因為真實檔案就是那樣；守門若只看得懂
「三欄都有車種名」那種版型，正常檔案會整批誤報。

方向切割那一條**先釘住事故本身**（`rows.length === 1`）再驗警告，
不然「有警告」可能只是碰巧。

**反證（實跑）**：把 `headerWarnings` 兩段整組拿掉 →
該紅的三條紅（車種重複、方向合併、轉向欄序），兩條對照組照舊綠。
