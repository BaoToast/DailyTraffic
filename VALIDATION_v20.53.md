# 全日交通量及車種組成 v20.53 驗證報告

- 驗證日期：2026-09-06
- 前一正式版：v20.49，commit `2da3e1c790a1508dbe9d580b424a03474dc4b38d`
- 待複查來源：Claude 提供的 v20.52 完整專案
- 本版性質：v20.52 複查修正版；保留 v20.50～v20.52 功能，另修正數字路名誤切與公開包識別資料。

## 一、複查結論

v20.52 的計算與主要操作測試均通過，但不宜原樣發布，原因有二：

1. 無分隔調查代號使用貪婪的 `\d{2,}`。檔名若為「案號＋T1501＋186縣道…」，
   會把路名開頭的 `186` 一起剝掉。
2. `app/traffic-data.json` 含可識別的計畫、11 個路段、方向與調查編號，並被打包
   進公開的 `assets/index-*.js`。既有守門未檢查 JSON 與發布說明。

因此發布版推進為 v20.53，不把上述風險帶到新的正式版。

## 二、本版修正

### 1. 無分隔檔名保留數字路名

- T 後代號只接受實際範例所需的 3 或 4 碼，例如 T601、T1501。
- 新增「代號後緊接 186縣道」的回歸測試。
- 有分隔符的舊格式、跨季名稱比對、平日／假日區分與調查點紀錄鍵維持不變。

### 2. 公開包匿名化

- `app/traffic-data.json` 的計畫名稱、擁有者、路段名稱、方向名稱與編號改為示範值。
- 1,056 筆紀錄的所有車流數字保持不變。
- 基準值仍為平日全日實際交通量 688,205 輛／日、24 小時 PCU 530,122 PCU／日。
- 更新說明與瀏覽器版面測試中的可識別文字一併改為示範值。

### 3. 防再發守門

- 資料外洩檢查擴及 `app/*.json` 與 `【更新說明】請先讀我.txt`。
- 新增內建基準資料結構檢查，要求計畫、路段、方向與編號均為匿名示範值。
- 測試用數字案號改為六碼，避免以寬鬆規則豁免任何可能的正式四或五碼案號。

## 三、刻意未變更

- 全日實際交通量計算
- 一般與轉向 PCU 係數及計算
- 24 小時 PCU、上午／下午／全日尖峰計算
- 路段 A/B 與路口多支線的駛入／駛出重分組
- 車種獨立分析與合併分類
- 混合時間格的挑選邏輯（仍只提示，不改計算）
- 專案隔離、重複匯入覆蓋、備份與還原

## 四、驗證結果

最終發布前實際執行結果：

- ESLint：0 錯誤；6 個既有 Hook 相依性警告。
- 單元／回歸：292 項中 291 通過、0 失敗、1 略過。
- 端對端：10 支瀏覽器腳本全部通過；涵蓋匯入、重複覆蓋、跨季名稱、
  日期警示、篩選、版面、結論草稿及可編輯 Excel 匯出。
- 真實批次檔全量名稱測試：測試檔未隨交付包提供，依設計略過；不宣稱已在本環境重跑。
- npm audit：全部相依套件為 24 項（1 low、7 moderate、16 high、0 critical）；
  僅正式執行相依套件為 2 項 moderate、0 high、0 critical，來源是 ExcelJS
  間接使用的 uuid。自動建議為跨主版降版 ExcelJS，可能破壞既有匯出，故本版
  不為消除警告而套用破壞性變更。

## 五、發布後核對

發布後須以帶版號或雜湊的路徑確認，不以固定檔名快取判斷：

- 首頁顯示 v20.53。
- v20.53 PDF 與 Word 手冊可下載。
- 首頁引用的新版雜湊資產可取得。
- GitHub Actions 測試與 Pages 發布均成功。

## 六、證據界線

- Claude 提供的三個 ZIP 與 SHA256SUMS.txt 已逐一核對一致。
- v20.52 基準測試在本機為 290 通過、0 失敗、1 略過；10 支瀏覽器腳本均通過。
- commit、Actions 與線上驗證結果須以發布後實際狀態為準；未完成前不宣稱
  發布成功。
