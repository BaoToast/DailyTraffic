# 全日交通量及車種組成 v20.37 驗證報告

- 驗證日期：2026-08-31
- 發布目標：`BaoToast/DailyTraffic` 的 GitHub Pages
- 前一正式版：v20.36

## 本版內容

1. 日別判讀改以真正的調查日期為優先；製表日期、列印日期、照片日期，以及「備註：假日不施測」等說明文字，不再覆蓋調查日別。
2. 有明確標籤的調查日期、可解析的未標籤日期，以及既有的「平日／假日交通量調查表」等結構化標題仍可正確判讀。
3. 全形數字字串會先正規化再作車輛數判讀；布林值不會被當成 1 輛，會依既有規則按 0 輛處理並列入檢核。
4. `--`、`－`、`—`、`–` 等「該轉向不存在」的合法占位符仍按 0 輛處理，不會誤報資料錯誤。
5. 本版只修正日別判讀與數值讀取邊界；交通量、PCU、尖峰、車種組成及方向／支線計算公式未變更。

## 複查中追加的修正

Claude 交付版已修正製表日期覆蓋調查日期，以及全形數字／布林值的讀取問題。複查時另發現第二階段日別備援仍可能把任意說明文字中的「假日」當成日別，例如只有「備註：假日不施測」的儲存格；現已限定為可解析日期或結構化日別標題。另補回合法 en dash（`–`）占位符，並同步更新系統日期、手冊、更新說明與忽略清單。

## 驗證結果

- 乾淨安裝：依 `package-lock.json` 安裝 587 個套件成功。
- TypeScript：`tsc --noEmit` 通過，0 錯誤。
- `npm test`：232/232 通過；建置成功，ESLint 0 錯誤（保留 7 項既有 React Hook 建議警告）。
- 端對端測試：匯入、結論草稿、版面寬度、日期與季度檢核、缺日期提醒、季別／調查月份切換等腳本全部通過，主控台無 JavaScript 例外。
- 多寬度版面：640、760、900、1024、1100、1280、1440、1500、1680、1920 px 均無水平溢出。
- 原 11 路段基準：平日全日實際交通量 688,205 輛/日、24 小時 PCU 530,122 PCU/日，與既有基準一致。
- PCU 黃金測試：機車、小型車、大型車、特種車及逐轉向既定係數全部一致。
- 新手手冊：PDF 40 頁、Word 31 頁，逐頁檢查無裁切、重疊、缺字或表格破版。
- GitHub Pages：由本次修正後原始碼重新建置；正式入口使用 `index-FSbWYlhx.js`、`index-Bg4Pleoq.css`、`exceljs.min-rL6nNlaR.js`、`jszip.min-Dg4CflgF.js`、`xlsx-Bme4aJTu.js`。

## 已知套件警示

`npm ci` 回報 23 項依賴警示（1 low、6 moderate、16 high），其中包含 npm 上仍停在 0.18.5 的 SheetJS。依既定維護原則，本版保留安全解析選項與原型污染偵測，不為了消除掃描警示而進行可能破壞舊版 Excel 相容性的強制升級。

## 結論

v20.37 的新增修正與既有計算均通過回歸驗證，可發布至 GitHub Pages。
