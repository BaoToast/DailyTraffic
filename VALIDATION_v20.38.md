# 全日交通量及車種組成 v20.38 驗證報告

- 驗證日期：2026-08-31
- 發布目標：`BaoToast/DailyTraffic` 的 GitHub Pages
- 前一正式版：v20.37

## 本版內容

本版是 v20.37 發布後的全面稽核結果，修正五項既有缺陷。**其中一項會改變使用者看到的數字**，其餘四項為標示、說明與清理。

1. **（會影響數字）還原備份時 PCU 係數未寫入。** `importBackup()` 把係數寫到 `activeProject`，但在「這台電腦一個計畫都還沒有」的還原路徑上，計畫是該函式自己剛依備份檔自動建立的，`activeProject` 當下仍是空字串（`setActiveProject` 是 React 狀態更新，同一個函式內讀不到新值——程式在 `targetProjectId` 上方本來就有這段註解，同一函式其餘 13 處寫入均已正確使用 `targetProjectId`，只有這兩處例外）。加上 `writeProjectPcuFactors()` 在 `projectId` 為空時 `return true`，整條路徑不會有任何失敗跡象：畫面顯示還原完成、係數也確實變成備份的值，但 `localStorage` 未寫入，重新整理後無聲退回預設值，全部 PCU 數字跟著改變（同一份資料實測尖峰 PCU 144.7 → 154.5，差 6.8%）。本版改用 `targetProjectId`，並把兩個寫入函式在無計畫時的回傳值改為 `false`，由呼叫端明確提示。
2. **歷季各表的單位只依「目前季度」標示。** `sheetActualUnit`／`sheetPcuUnit`／`pcu24Label`／`trendActualUnit`／`trendPcuUnit` 全部源自 `surveyScope`，而 `surveyScope` ← `filtered` ← `scoped`，帶有 `r.quarter === quarter` 條件；但 `historicalDailyRows` 走的是未經季度篩選的 `analysisRecords`。因此三張跨季度工作表（歷季全日交通量、歷季車種組成、歷季趨勢）會被整張標成當季單位：選 24 小時季時，只調查 4 小時的季度被標成「全日實際交通量（輛/日）」並排在真正的全日量旁邊；選部分時段季時，真正的 24 小時資料被標成「輛/調查時段」。本版標題改用中性單位「輛」「PCU」，並新增逐列的「調查涵蓋」欄（`coverageLabelOf()`，逐季度×日別×調查點判定），納入自動篩選範圍。
3. **匯出範圍說明與實作不符。** 匯出視窗宣稱「歷季全日量與趨勢」依歷季分析面板的 `trendMode`／`trendRoad` 篩選，實際上 `historicalDailyRows`／`historicalCompositionRows` 完全不吃這兩個條件（刻意如此，兩張是明細底稿），只有歷季趨勢與歷季組成圖表資料才吃。說明已照實作重寫。
4. **`anomalyQuarters` 自寫季度排序。** 舊寫法僅將兩碼年份補零後做字串比較，同一計畫混用民國與西元標記時會排錯（`2025Q1` 即民國 114Q1，應落在 `113Q4` 與 `114Q3` 之間，實際被排到最後）。改用既有的 `compareQuarters()`，全系統只留一套季度先後規則。
5. **`deleteProject()` 殘留依計畫存放的 localStorage。** 計畫 id 為 `crypto.randomUUID()`（`app/api/projects/route.ts`），不會重複，故不致被新計畫誤用；但會持續占用瀏覽器儲存空間，而儲存空間不足正是第 1 項提示訊息所指的失敗原因之一。新增 `PROJECT_SCOPED_KEYS` 與 `dropProjectScopedStorage()`，日後新增依計畫存放的鍵只需加入清單。

## GPT 複查補強

1. Claude 已修正 PCU 係數寫入的回傳值與目標計畫，但複查發現 `importBackup()`、`resetPcuFactors()` 與 `applyProjectTemplate()` 後續仍會再顯示固定的成功訊息，造成儲存失敗警告被覆蓋。現改為累積並在流程結束時一次呈現，且範本的車種分類、路口設定與備份中的結論條件範本也一併回報寫入失敗。
2. Claude 為歷季趨勢新增單一「調查涵蓋」欄，但同一季度的平日與假日可能使用不同調查時數，單一欄位無法對應兩個數值欄。現改為「平日調查涵蓋」及「假日調查涵蓋」兩欄；多調查點涵蓋不同時也明確標示，不再任取其中一個值。
3. 新增守門測試與端對端情境，刻意讓同一季度的平日／假日使用不同調查涵蓋，以驗證匯出欄位與折線圖數值來源沒有錯位。

## 稽核中確認「不需修正」的項目

- **備份不還原異常門檻**：刻意設計，程式內有明確註解（門檻與範本屬「使用者目前的設定」，不隨某一季度的舊備份改動）；且結論條件範本實際上**有**還原，採合併寫回以免蓋掉本機既有範本。維持原狀。

## 驗證結果

- TypeScript：`tsc --noEmit` 通過，0 錯誤。
- ESLint：0 錯誤，7 項既有 React Hook 建議警告（與 v20.37 相同，無退步）。
- 單元／回歸測試：245/245 通過（v20.37 為 232 項，本版新增 13 項守門測試）。
- **守門測試已證明對未修正的 v20.37 原始碼會失敗**：以 v20.38 的測試檔搭配 v20.37 的 `DashboardClient.tsx` 執行，原始 11 項中 9 項失敗（其餘 2 項為純函式行為測試，其中季度排序一項另含一條斷言，直接證明舊比較器會排錯）；複查再補入 2 項，防止成功訊息覆蓋儲存失敗警告及平／假日涵蓋欄位退化。
- 端對端測試：全部通過，瀏覽器主控台無 JavaScript 錯誤；另以同一季度平日／假日調查時數不同的情境，確認歷季 Excel 的兩個涵蓋欄位與圖表數值沒有錯位。
- 使用手冊版面：PDF 41 頁、Word 31 頁逐頁檢視，未發現文字裁切、重疊、缺字或表格溢出。
- 原 11 路段基準：平日全日實際交通量 688,205 輛/日、24 小時 PCU 530,122 PCU/日，與既有基準逐位一致。
- PCU 黃金測試：機車、小型車、大型車、特種車及逐轉向既定係數全部一致。
- 三支系統共用的 `period-date` 模組未變動，仍與路口轉向、交通服務水準保持同步。

## 已知套件警示

`npm ci` 回報 23 項依賴警示（1 low、6 moderate、16 high），其中包含 npm 上仍停在 0.18.5 的 SheetJS。依既定維護原則，本版保留安全解析選項與原型污染偵測，不為了消除掃描警示而進行可能破壞舊版 Excel 相容性的強制升級。此項與本版計算無關。

## 結論

v20.38 修正一項會改變數字的靜默錯誤與四項標示／清理問題，計算口徑未變更，回歸與黃金值全數通過，可發布至 GitHub Pages。
