# 全日交通量及車種組成 v20.47 驗證報告

- 驗證日期：2026-09-05
- 前一正式版：v20.42（commit `5b93cb8c290ad360a838a7760108dfa40368a85e`）
- 複查基準：Claude 交付的 v20.46
- 本版性質：補齊 v20.46 已宣告但未完整實作的分日顯示與拖放防護。

## 複查發現與修正

### 一、「平日＋假日」只有明細分日，其他區塊仍相加

v20.46 的路段／路口明細已分成平日與假日兩列，但首頁全日交通量及
24 小時 PCU、路段排名、跨計畫比較、結論草稿，以及部分可編輯 Excel
仍將兩個調查日相加。這會把不存在的「兩日合計」放在標示為每日的欄位。

v20.47 以每列自己的 `dayType` 分組，讓以上區塊全部逐日顯示；跨計畫
比較也依各計畫、各日分列。可編輯 Excel 的路段、路口與跨計畫資料及
圖表分類名稱均帶日別，避免兩列同名。

### 二、v20.46 說明宣稱的全頁拖放保護實際缺漏

v20.46 交付說明寫明檔案掉在放置區外會被攔截，但原始碼沒有全域
`dragover`／`drop` 監聽，原有 `e2e-progress.mjs` 也未驗證這一點。
v20.47 已補上保護與三項瀏覽器檢查；放置區內維持原匯入行為，一般文字
拖曳不受影響。

## 計算邊界

- 未修改 Excel 解析、單一平日／假日交通量、PCU、尖峰挑選、車種歸類、
  方向分組或備份資料格式。
- 「平日＋假日」的語意統一為同時呈現兩個調查日，不再建立兩日合計值。
- 車種組成面板仍可用兩天合計作為比例分母，並明確標示「平假日兩天合計」。

## 已完成的驗證

- v20.46 原始基準：ESLint 0 錯誤、280/280 單元與回歸測試通過。
- 本次定向單元檢查：52/52 通過。
- 拖放及逐檔進度端到端檢查：全部通過。
- 平假日分日與多選端到端檢查：全部通過；切回平日後關鍵數值完全一致。
- 最終 `npm test`：281/281 通過、0 失敗、0 略過；ESLint 0 錯誤、
  6 項既有 Hook 相依性警告。
- 最終 `npm run e2e`：8 支腳本全部通過，沒有 JavaScript 例外。
- 重新安裝鎖定相依套件後再跑 `npm test`：281/281 通過。
- GitHub Pages 根目錄與乾淨重建產物：HTML、CSS 及 4 支 JavaScript
  資產逐位元相同。
- 手冊：v20.47 PDF 與 Word 已重新產生，`manuals/` 與
  `public/manuals/` 副本雜湊一致。
- 安全掃描：已將可無破壞更新的 `brace-expansion` 鎖定至 1.1.18，
  高風險警示歸零。仍有 ExcelJS 4.4.0 間接使用 UUID 8.3.2 的 2 項
  中風險警示；掃描工具提出的自動修法是降級 ExcelJS 至 3.4.0，屬破壞性
  變更，未為清除警示而降低匯出相容性。

## 發布後證據

- 功能發布 commit：`3e6a3bd5d9882c20a2cc31dc3a5b918956f11880`。
- GitHub Actions「建置與測試」run `33941646403`：成功。
- GitHub Pages「pages build and deployment」run `33941645715`：成功。
- 公開首頁以快取參數重新讀取後，引用本版主資產 `assets/index-fY1Kqilq.js`；
  該雜湊資產內含 `v20.47`、分日顯示與拖放防護程式。
- `manuals/Traffic_Analysis_Beginner_Guide_v20.47.pdf` 與
  `VALIDATION_v20.47.md` 均回傳 200。
- v20.46 舊手冊與舊主資產 `assets/index-Bu23c4Qj.js` 均回傳 404。
