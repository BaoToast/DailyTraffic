# 全日交通量及車種組成 v20.36 驗證報告

- 驗證日期：2026-08-31
- 發布目標：`BaoToast/DailyTraffic` 的 GitHub Pages
- 前一正式版：v20.35

## 本版內容

1. 日期／時間格式的車輛計數格不再被轉成 epoch 毫秒；其數值按 0 輛處理，並在匯入確認畫面列出來源檔、工作表與儲存格位置。
2. `--`、`－`、`—` 等「該轉向不存在」的既有合法記號仍按 0 輛處理，不會誤列為資料錯誤。
3. 同一日別拆成多張交通量工作表時全部讀取；照片、時相、監測日誌及說明頁才略過。
4. 非資料頁改採完整名稱判斷，避免把「平日-號誌化路口」等真正的交通量工作表誤排除。
5. 單一檔案或部分檔案讀出 0 列、日別只能依檔名推測時，匯入畫面會逐檔提出警告。
6. 零流量異常改以實際涵蓋時數計算，不再把每一筆時間格直接當成 1 小時。
7. TypeScript 專案檢查已恢復為 0 錯誤；未改動任何交通量、PCU、尖峰、車種組成或方向／支線計算公式。

## 複查中追加的修正

Claude 原交付版已加入 `isUnusableCount`，但正式匯入流程沒有使用它，因此日期格雖歸零，使用者仍看不到警告。本次把原始資料警告接到匯入檢核畫面，並新增解析層回歸測試。另將非資料工作表的寬鬆關鍵字排除改為完整名稱判斷，避免真實資料分頁因名稱含「號誌」而被略過。

## 驗證結果

- 乾淨安裝：依 `package-lock.json` 安裝 587 個套件成功。
- TypeScript：`tsc --noEmit` 通過，0 錯誤。
- `npm test`：228/228 通過；建置成功，ESLint 0 錯誤（保留 7 項既有 Hook 建議警告）。
- 端對端測試：匯入、路段／路口、PCU、車種、Excel 匯出、日期期別、備份、版面寬度等 4 支測試腳本全部通過，主控台無 JavaScript 例外。
- 原 11 路段基準：平日全日實際交通量 688,205 輛/日、24 小時 PCU 530,122 PCU/日，與既有基準一致。
- PCU 黃金測試：機車、小型車、大型車、特種車及逐轉向既定係數全部一致。
- GitHub Pages：由本次修正後原始碼重新建置，根目錄入口與資產已同步。

## 已知套件警示

`npm ci` 回報 23 項依賴警示（1 low、6 moderate、16 high），其中包含 npm 上仍停在 0.18.5 的 SheetJS。依既定維護原則，本版保留安全解析選項與原型污染偵測，不為了消除掃描警示而進行可能破壞舊版 Excel 相容性的強制升級。

## 結論

v20.36 的新增修正與既有計算均通過回歸驗證，可發布至 GitHub Pages。
