# 全日交通量及車種組成：工程交接基準

> 本文件是本程式長期維護的正式工程交接基準，不是聊天摘要。未來的主要 GPT 必須先用實際 Repository 核對身分，再完整閱讀本文件。若本文件、聊天、README 與實際程式或可重現測試衝突，以目前可驗證的程式碼、Git 與測試為準，並更新本文件。

## 1. 專案身分與正式發布基準

| 項目 | 已核對內容 |
| --- | --- |
| 正式名稱 | 全日交通量及車種組成 |
| 英文／套件識別 | DailyTraffic；`traffic-volume-vehicle-composition` |
| 用途 | 匯入路段或路口全日交通調查 Excel，進行全日交通量、PCU、尖峰小時、方向／路口流向、車種組成、歷季與跨計畫比較、品質檢查及可編輯 Excel 報表等分析 |
| 本機資料夾 | `DailyTraffic` |
| 本機完整路徑（2026-09-13 實際核對） | `D:\Users\95108\Downloads\2026-08-30\files-mentioned-by-the-user-gpt\work-v20.62-review\candidate\DailyTraffic` |
| GitHub Repository | `BaoToast/DailyTraffic` |
| Repository URL／origin | `https://github.com/BaoToast/DailyTraffic.git` |
| GitHub Pages | `https://baotoast.github.io/DailyTraffic/` |
| 正式部署方式 | GitHub Pages，由 Repository 根目錄發布；GitHub Actions 只測試、不部署 |
| branch | `main`（本機已追蹤 `origin/main`） |
| 交接前正式程式 commit | `f77328c42131452bccdd3ef5da125da5ab8b9bf8`，`Release DailyTraffic v20.63` |
| 正式版本 | `v20.63`；程式版本來源為 `app/system-release.ts`，更新日 2026-09-09 |
| 本文件 commit | 以包含本檔的最新 `git log -1` 為準；Git commit 無法在同一個 commit 內容中自我記錄自己的雜湊 |

### 三套交通程式的界線

本對話及本 Repository **只負責「全日交通量及車種組成」**。另外兩套獨立程式是「旅行速率與服務水準」及「路口尖峰轉向交通量」，不得因功能或名稱相似而在其他 Repository 修改、提交或發布。本專案可借鏡其他系統的概念，但未經使用者明確要求，不得重構、合併三套系統，也不得改變已確認的本系統計算規則。

## 2. 目前完成程度與正式使用情境

系統主體已完成，現階段工作以錯誤修正、缺少功能補充、計算驗證、回歸防護及發布維護為主。正式版本是免登入、瀏覽器本機儲存的 GitHub Pages 單機版，可管理多個計畫、季度、路段與路口資料，並以備份檔在不同電腦間移轉。

已完成的重要能力包括：

- 路段 A／B 雙方向格式與路口 3 至 7 支線格式的匯入、辨識及分析。
- 批次、單筆與拖放匯入；同季逐筆匯入不應互相清空，不同檔案以共同資料身分規則判定新增或取代。
- 平日、假日及平日＋假日檢視；平假日不得相加成不存在的「一天」數值。
- 全日實際車輛數、一般 PCU、尖峰 PCU、方向／駛入／駛出、各車種數量與比例。
- 動態車種：原始檔出現超過四個車種時可獨立分析，或由使用者歸類到四個核心車種。
- 一般 PCU 與各車種轉向 PCU 可由使用者依計畫調整。
- 路口幾何角度、對向支線與左轉／直行／右轉自動判定及人工覆核。
- 歷季趨勢、跨計畫比較、報告草稿、結論範本、自訂比較報表、資料品質與定稿流程。
- 可編輯 Excel 報表、JSON 完整備份／還原、新手 PDF／Word 手冊。
- 本機 IndexedDB 永久保存；重新整理或關閉瀏覽器後資料應保留。

不具備、也不得誤稱已具備：

- GitHub Pages 沒有中央雲端資料庫、即時跨電腦同步或多人共同編輯。A、B 電腦之間須用「匯出備份／匯入資料」移轉。
- 沒有伺服器端帳號權限與雲端計畫共享。畫面中的專案分開管理是本機資料隔離，不是雲端權限隔離。
- 本系統的 GPT Site 已刪除。除非使用者日後明確要求重建，禁止部署或回報 GPT Site。
- 三套交通系統整合成智慧分析平台屬遠期構想，已決定暫緩；現階段不得為此改寫現有架構或公式。

## 3. 整體架構與主要檔案

正式 GitHub Pages 的主要資料流如下：

```text
Excel 檔案
  -> DashboardClient 匯入流程
  -> traffic-parser（格式／Sheet／儲存格／車種／方向／轉向解析）
  -> period-date、road-identity、partial-day（期間、名稱、時段正規化）
  -> final-workflow（品質、身分、重複資料、異常、定稿狀態）
  -> app-fetch + IndexedDB（計畫、調查資料與設定）
  -> period-analysis / vehicle-analysis / intersection-flow（交通工程計算）
  -> DashboardClient / trend-script / chart-notes / conclusion / report-draft
  -> 畫面、圖表、Excel 報表、JSON 備份
```

主要模組與責任：

- `app/DashboardClient.tsx`：前端總協調器。管理計畫、匯入對話框、篩選、品質與定稿、資料彙整、圖表、Excel 匯出和備份／還原。此檔影響面廣，變更通常至少是中風險。
- `app/traffic-parser.ts`：Excel 安全讀取、路段／路口格式辨識、Sheet 與資料格解析、動態車種、日期、轉向及目的支線資料。Parser 變更原則上是高風險。
- `app/road-identity.ts`：編號／名稱正規化、全半形與分隔符處理、日期與多餘字詞移除、別名及穩定身分。它決定跨季是否合併成同一路段／路口。
- `app/period-date.ts`：民國／西元季度與月份解析、調查日期合法性、檔案日期與管理季度一致性檢查。
- `app/partial-day.ts`：時段解析、覆蓋率、60 分鐘移動尖峰、上午／下午界線、部分時段及混合時間格處理。
- `app/vehicle-analysis.ts`：動態車種、分類映射、原始數量、一般與轉向 PCU 計算，維持單一 PCU 計算來源。
- `app/intersection-flow.ts`：3 至 7 支線幾何、角度、對向、左／直／右判定、人工修正、駛入目的支線推導及拓撲稽核。
- `app/period-analysis.ts`：全日、24 小時、AM／PM 尖峰、路段方向、路口支線、車種數量／比例／PCU、尖峰範圍與匯出資料表。
- `app/final-workflow.ts`：匯入驗證、資料身分、品質狀態、異常、定稿、範本、完整度與版本歷程。
- `app/trend-script.ts`：歷季七項趨勢指標、缺季斷點、民國年跨年、跨計畫加權值、敘述及座標軸。
- `app/chart-notes.ts`：只根據已算好的顯示資料產生圖表說明，不得另做一套交通計算。
- `app/conclusion.ts`：結論產生器、選項及範本。
- `app/report-draft.ts`：分析報告草稿章節與趨勢敘述。
- `app/app-fetch.ts`：GitHub Pages 離線 API 相容層，使用 IndexedDB `traffic-analysis-github-pages`（版本 2）。
- `app/workflow-store.ts`：品質／定稿工作流程 IndexedDB `traffic-analysis-workflow`；以共用佇列、入列前快照、失敗後可續行、按計畫隔離，避免重新整理或快速操作造成寫入競態。
- `app/page.tsx`：應用入口，可接受使用者資訊但不要求登入；由 Error Boundary 保護 Dashboard。
- `db/schema.ts`、`app/api/*`、`worker/*`：可選的伺服器／D1 架構骨架，不是目前正式 GitHub Pages 執行路徑。不得把它們當作已上線雲端後端。
- `tests/*`：單元、回歸、契約、資產及發布守門測試。
- `scripts/e2e-*`：Playwright E2E 與瀏覽器驗證。
- `scripts/manual/*`：新手手冊產生流程。
- `app/system-release.ts`：正式顯示版號和更新日期的唯一主要來源；版本改動須同步所有發布中繼資料及手冊／驗證檔名。

### 重要依賴與環境

- Node.js：`>=22.18.0`；CI 使用 Node 22。
- npm：唯一套件管理器；CI 固定 npm `11.6.2`，使用 `npm ci`。不得加入或維護 pnpm lock/workspace。
- TypeScript `5.9.3`、React／ReactDOM `19.2.6`、Vite `8.0.13`、vinext beta。
- Excel：ExcelJS `4.4.0`、JSZip `3.10.1`，另有本機 vendor 的 xlsx `0.20.3`。
- Playwright `1.62.1` 用於 E2E。
- `.openai/hosting.json` 雖然目前不部署 GPT Site，但 `vite.config.ts` 直接匯入它；刪除會讓 build 失敗，必須保留。
- `.gitattributes` 的 `* -text` 用來避免 Windows 自動改寫已建置資產位元組；不得輕易移除。
- `README.md` 目前仍帶有通用 vinext／可選 Sites-D1 說明，並非本系統完整維護事實來源。這是文件債；在另有明確任務前不要因交接任務順手改寫。

## 4. 輸入、Parser、資料驗證與已知陷阱

### 支援的調查型態

1. **路段全日交通量**：通常有方向 A、方向 B。畫面及明細保留兩方向的輛數、PCU、方向尖峰及全方向同時段尖峰。
2. **路口全日交通量**：通常有路口 A 至 G，不使用路段 A／B 名稱。依使用者選擇顯示駛入、駛出或兩者；支援非十字、三叉至七叉路口。
3. **動態車種格式**：不能假設永遠只有四種車。Parser 應保留原始車種名稱，再由車種管理決定獨立分析或映射。

CSV 是否涵蓋所有 Excel 能力目前沒有等同真實檔案的完整證據；若新增或變更 CSV，須標示為待驗證並補測試，不得只因介面或通用程式碼存在就宣稱完整支援。

### 儲存格與數量規則

- 所有原始車數只經 `cellCount()` 進入：先做 NFKC 正規化。
- Date、布林、非有限數字與非數字字串不得當交通量。
- 空白及破折號占位視為 0；非空但無效的值發出警告後按 0 處理。
- 原始顯示車數以 `Math.round` 四捨五入為整數。這是現行已核准行為，若要改成拒絕小數或保留小數，屬核心規則變更。
- Excel 安全選項禁止公式計算、HTML 與 VBA；不得為方便解析而重新啟用不受信任的巨集或 HTML。
- Parser 及名稱／備份處理含 prototype-pollution 防護；任何以欄位名當物件 key 的新程式都需保留相同防護。

### 車種原始數量優先順序

動態車種計數依序取：

1. 有 `vehicleCounts` 時使用它；
2. 有 `turnData` 且其合計與舊欄位差異小於 1 時，使用轉向明細總和修補舊版四捨五入差；
3. 否則使用四個核心車種欄位。

不得在 UI、圖表或匯出各自重算一套車數，否則會再次產生顯示與匯出不一致。

### 路段／路口身分與重複匯入

- 名稱正規化應處理 NFKC、空格、全半形、`-`／`~` 等分隔符、日期及檔名尾碼。
- 同一路段仍不確定時，UI 應讓使用者建立新路段或併入既有路段，並可手動修正顯示名稱／別名。
- 驗證預覽與真正儲存必須使用同一 `trafficIdentity`。同一身分的新檔是確認後取代，不是相加；不同身分則新增。
- 已定稿季度不得未經明確流程覆蓋。
- 逐筆匯入同一季時，不得以整季新陣列覆蓋其他已匯入檔案。

### 日期與季度

- 民國年與西元年會正規化到內部標準季度；例如 `2026Q1` 對應 `115Q1`。
- 檔名、工作表、調查日期與使用者選擇季度的矛盾應顯示警告／要求確認，不得靜默猜測。
- 歷季趨勢若混用無法可靠對應的曆法格式，不得任意串接。
- 缺少季度是 `null`／圖表斷點，不是 0。

### 時段與部分日資料

- 完整 24 小時使用「輛/日、PCU/日」。不完整資料使用「輛/調查時段、PCU/調查時段」。
- 混合時間格目前是**刻意只警告、不改變既有挑選邏輯**。除非使用者明確批准並有新規格與回歸案例，不得順手更改。
- 空白、缺時段、間斷與跨日資料需保留品質警告；不得補造零值使曲線看似完整。

## 5. 交通工程計算規則

### 預設一般 PCU

| 核心車種 | 一般 PCU |
| --- | ---: |
| 機車 | 0.5 |
| 小型車 | 1.0 |
| 大型車 | 1.5 |
| 特種車 | 2.5 |

### 預設轉向 PCU

| 核心車種 | 直行 | 右轉 | 左轉 |
| --- | ---: | ---: | ---: |
| 機車 | 0.3 | 0.4 | 0.5 |
| 小型車 | 1.0 | 1.3 | 1.5 |
| 大型車 | 1.5 | 2.0 | 2.3 |
| 特種車 | 2.0 | 2.3 | 2.5 |

以上是預設值，不是不可改的法規常數。使用者可按計畫調整；新偵測車種的初始係數為 1，可獨立分析或映射到核心車種。係數只要求是有限數字，**不得增加最小值、步進或正值限制**。0 會使該項 PCU 歸零，負值會反向計算，畫面須警告，但不阻擋使用者自訂。

### PCU 唯一計算原則

- 路口且有轉向明細：`PCU = Σ（各車種各轉向車數 × 該車種該轉向係數）`。
- 路段或無轉向明細：`PCU = Σ（各車種車數 × 一般 PCU）`。
- 映射到核心車種後，使用該計畫**目前**核心車種係數；不得沿用匯入當時過時快照。
- 獨立自訂車種使用自己的係數；缺少必要係數時結果按現行規則為 0 並顯示警告，不可偷偷套用任意預設。
- PCU 的資料來源應集中在 `vehicle-analysis.ts`／相關共用邏輯，UI、趨勢、結論、Excel 不得另複製公式。

### 尖峰小時

- 尖峰以 PCU 決定；若所有 PCU 都是 0，才以實際車數判定；全為 0 時段顯示「—」。
- 60 分鐘以上／每筆已為小時格：選最大合格時段。
- 小於 60 分鐘格：使用連續資料的 60 分鐘移動窗，不得跨資料缺口或平／假日邊界拼接。
- AM／PM 尖峰窗必須完整落在各自邊界內。
- 同值時使用固定且可重現的較早時段，不得因物件順序或瀏覽器不同而改變。
- 「調查點／全方向」模式：先找單一共同尖峰時段，再呈現各方向在該時段的值，因此各方向可以相加到總值。
- 「各方向」模式：每個方向各自找尖峰，時間可能不同，**不得相加聲稱是同一時段總尖峰**。
- 尖峰單位只在完整 60 分鐘時使用「輛/hr、PCU/hr」；不足 60 分鐘必須揭示實際時長。

### 路段、路口與轉向

- 路段保留 A／B 雙向規則。
- 路口依支線 A 至 G 處理，不得硬編成東西南北或固定四支線。
- 路口位置由角度、對向支線及可人工修正的左／直／右關係決定。
- 可用 origin（駛出）、destination（駛入）或兩者檢視；駛入與駛出雖分配不同，同一範圍總車數應守恆。
- 自動轉向判定是輔助，人工覆核優先；拓撲不完整或矛盾時必須警告，不得以猜測結果當已確認。

### 車種比例、歷季與跨計畫

- 車種比例分母必須是同一計畫、季度、日別、路段／路口、方向／流向與調查範圍的有效車數合計。
- 分母為 0 時依呈現語意顯示 0.0% 或無資料；不得產生 Infinity／NaN。
- 平日與假日各自保留序列。選擇「平日＋假日」是並列／比較，不是把兩天加成單日。
- 歷季缺資料用空值斷線，不以 0 取代。
- 跨計畫彙整指標須使用程式既有的加權／分母規則，不得把百分比直接算術平均。

### 精度與四捨五入

- 原始車數在匯入入口按前述規則成為整數。
- PCU 計算過程保留小數，不得在每一步提前四捨五入。
- 顯示與 Excel 依欄位格式在最後一步格式化（常見為 0 或 1 位小數）；若修改位數，必須同步 UI、圖表、匯出及測試。

## 6. 不可任意改變的系統不變條件

1. 平日、假日、缺季、部分日資料必須保持語意，不得以 0 或相加掩蓋缺資料。
2. 原始車數、PCU、尖峰、圖表、結論與 Excel 必須共享同一資料來源與計算結果。
3. 路段與路口格式皆須回歸；修正一種格式不能破壞另一種。
4. 專案、季度、資料、PCU、別名、路口幾何及工作流程狀態須按計畫隔離。
5. 重複匯入是明示確認後取代相同資料，不是重複累加；逐筆新增不得清掉同季其他資料。
6. IndexedDB 寫入必須等待 transaction 完成；讀取必須等待尚未完成的寫入。不得恢復只等 request success 的競態寫法。
7. 動態車種不能在分類後遺失原始名稱、原始數量或可追溯性。
8. 路口不得假設固定四支線、固定方位或用 A/B 路段介面。
9. PCU 係數允許使用者輸入任意有限值，包括 0 與負值；警告但不阻擋。
10. 正式部署只以 GitHub Pages 為準；不得因保留 `.openai/hosting.json` 而誤報 GPT Site 存在。

## 7. 本機保存、備份與還原

- Pages 資料存在瀏覽器 IndexedDB，正常重新整理、關閉分頁或關機後再開仍應保留；清除瀏覽器網站資料、無痕模式、換瀏覽器或瀏覽器損壞仍可能遺失。
- `app-fetch.ts` 儲存計畫／調查資料；`workflow-store.ts` 儲存品質及定稿狀態。兩者資料庫不同，備份與刪除流程需涵蓋完整狀態。
- JSON 備份格式：`traffic-analysis-backup`，目前 schema version 5。
- 備份包含計畫、PCU、轉向 PCU、動態車種映射、別名、路口設定、工作流程、結論範本與調查紀錄。
- 還原時驗證必要字串與季度、將西元季度正規化成民國、沒有計畫時可建立新計畫。
- 已定稿季度禁止任意覆蓋；重疊資料須確認後取代，不相加。
- 只還原備份內季度的工作流程狀態；全域門檻／範本按既有規則保存，結論範本按名稱合併，歷程保留上限 10。
- IndexedDB 寫入失敗或空間不足必須顯示警告，不能回報成功。
- 跨電腦使用的正式方法是 A 電腦匯出 JSON、B 電腦匯入；不是即時同步。
- 刪除計畫／季度屬破壞性操作，必須要求確認，建議先備份。

## 8. UI／UX 與輸出決策

- 免登入即可使用。GitHub Pages 右上角是本機使用者語意，不得誤導為雲端帳號。
- 左側可管理多計畫並勾選跨計畫比較；計畫名稱過長需截斷／換行而不能撐壞版面。
- 路段使用方向 A/B；路口依實際支線顯示駛入／駛出，相關篩選和明細欄位必須同步。
- 所有主要數值附單位；部分日、尖峰窗和平假日語意必須在標題或說明中看得出來。
- 圖表應保持專業、無文字重疊、無遮住資料表；Excel 圖表須為可編輯的原生圖表並連結數值範圍。若舊 Excel 不支援某項原生圖表，需提供數值表和清楚提示。
- 圓環圖避免把長標籤全部塞在扇區內；使用圖例／旁側數值區顯示車種、數量和比例。
- Excel 匯出必須帶出繪圖用資料、單位與可追溯明細，不可只嵌入圖片。
- 新手手冊是正式功能；手冊連結必須帶版本號，且發布時同步 DOCX、PDF 與程式版號。
- 異常、資料不完整、日期矛盾、混合時間格、係數異常與儲存失敗要可見，不能靜默修正或默認正常。

## 9. 重要技術決策與曾放棄方案

### 保留的決策

- **單機離線優先**：目前使用者接受本機永久保存加備份移轉，避免在系統驗證完成前引入雲端帳號／資料庫複雜度。
- **每計畫係數與設定隔離**：不同計畫可能採不同標準，不能共用一組會互相污染的設定。
- **共用計算、末端呈現**：圖表說明、匯出、結論不應各自重算交通量。
- **以原始資料可追溯為核心**：即使映射車種或修正別名，仍保留原始車種與來源記錄。
- **GitHub Pages 根目錄發布**：`npm run build:pages` 產生 `github-pages/dist`，發布前將內容同步到 Repository 根目錄並刪除舊 hash 資產。
- **CI 只測試**：`.github/workflows/ci.yml` 在 main、PR 或手動觸發執行 Node 22 + npm 11.6.2 + `npm ci` + `npm test`，不負責 Pages 發布。

### 已證明有問題或已放棄的方向

- **依靠 session／記憶體保存匯入資料**：重新整理會消失，已改成 IndexedDB。
- **只等 IndexedDB request success**：transaction 尚未完整落盤會造成讀取競態，已改為序列化並等待完成。
- **同季逐檔匯入直接替換整季陣列**：會讓前一檔消失，必須以共同身分合併／取代單筆。
- **只支援固定四車種**：無法處理大貨車、大客車、聯結車等，已改為動態車種和映射。
- **把路口簡化成東西南北或固定十字**：三至七叉會失真，已改為角度、對向和人工轉向覆核。
- **在 Excel 圓環內放全部文字**：容易重疊，改採清楚圖例及旁側數值表。
- **Excel 只匯出圖片**：無法編輯與跟隨數值，改為原生圖表和來源表。
- **保留 pnpm 第二套鎖檔**：專案與 CI 均使用 npm，雙鎖檔易漂移，已移除。
- **用固定 URL、`package.json` 或舊快取判斷線上版本**：不可靠；必須用帶版號手冊、`?v=` 或新版唯一資產 hash 驗證。
- **直接移除 `.openai/hosting.json`**：即使 GPT Site 不存在，Vite 設定仍匯入它，移除會使 build 失敗。
- **把平日與假日直接相加**：產生無實務意義的單日值，明確禁止。
- **為混合時間格順手改尖峰挑選**：目前規格只警告，改計算會造成未批准 regression。

## 10. 重要歷史 Bug 與回歸注意事項

以下是後續 reviewer 應優先知道的缺陷族群；詳細每版內容仍可查 Git history 與 `【更新說明】請先讀我.txt`。

| 版本族群 | 現象／Root cause | 修正與回歸重點 |
| --- | --- | --- |
| v20.24–20.25 | 路口流量視角標籤及駛入／駛出總數不一致 | 統一流向語意；驗證各支線分配和總量守恆 |
| v20.28–20.31 | 發布包無法在他機重建、隱私／換行／lint 與多路口問題 | 完整專案包必須含建置所需目錄；乾淨目錄重建；不要只測現成 node_modules |
| v20.32–20.33 | 名稱合併、方向名稱、路口資料保存、安全及車數／動態車種／PCU 重複實作 | 保留原始資料；共同身分；`cellCount` 與單一 PCU 來源不得分叉 |
| v20.34–20.42 | 調查日期與季度、民國／西元、缺季、圖表、手冊與 release metadata | 日期衝突要警告；版本／手冊／驗證／資產同步；缺季為 gap |
| v20.43–20.48 | 平日＋假日、多選匯入、進度、路口視角、檔案選取回饋 | 平假日分列；drop/picker 都要防重；道路計畫不得顯示無效路口選項 |
| v20.49–20.54 | 單位一致性、混合時間格、檔名數字、無效／空泛測試 | 顯示與匯出單位一致；混合格只警告；測試必須真的斷言結果 |
| v20.55–20.60 | 三叉路口、趨勢指標、缺季斷線 | 幾何／轉向覆核；七項趨勢資料流；缺值不可變零 |
| v20.61–20.62 | 圖表座標、分區排版、圖表說明、對比、工作流程儲存競態 | 說明不得重算；視覺可讀；IndexedDB 共用佇列與 project isolation |
| v20.63 | 風險導向複查發現持久化、趨勢、報表、圖表與發布守門缺口 | 358 pass／0 fail／1 skipped；持久化寫入完成、缺季、平假日、報表／圖表與 release guard 均須保留 |

最容易復發的問題：Parser 改動只驗新格式、UI 篩選改動未同步 Excel、平假日或缺季被 0 污染、路口 A-G 被硬編四支線、每計畫設定錯用全域值、IndexedDB 寫後立即讀出舊資料、發布根目錄還留舊 hash 資產、畫面版號與手冊／驗證檔名不同。

## 11. 測試、Build 與最後實際驗證證據

### 主要指令

- `npm ci`：以 lockfile 建立乾淨依賴。
- `npm test`：lint、production build 及 Node 測試總流程。
- `npm run lint`：ESLint，設定為 0 warnings。
- `npm run test:unit`：Node 單元／回歸測試。
- `npm run build`：production build。
- `npm run build:pages`：GitHub Pages 建置到 `github-pages/dist`。
- `npm run e2e`：完整 Playwright／樣本／Pages E2E 流程。

### v20.63／`f77328c` 已實際完成的驗證

- 乾淨完整單元／回歸：359 項，358 通過、0 失敗、1 skipped。
- 完整 E2E：14 個 suites／scenarios、316 checks，全數通過，無 JavaScript error。
- TypeScript、lint（0 warnings）、production build、Pages build：通過。
- GitHub Actions「建置與測試」：對 `f77328c` 成功。
- GitHub Pages deployment：對 `f77328c` 成功。
- 線上 cache-busted 驗證：根頁引用 `assets/index-Bx-GX3tF.js`、`assets/index-D4nDRD8q.css`；v20.63 版號手冊、驗證檔及主資產皆回應 200。
- 詳細證據在 `VALIDATION_v20.63.md` 與發布 commit 中。

上述是 **v20.63 正式程式 commit 的既有實測證據**。建立本交接文件是純文件變更，合理檢查為 Git diff、Markdown／路徑一致性與乾淨 push；不應把沒有重新執行的完整測試說成在交接 commit 又跑一次。

### 依賴安全狀態

- `npm audit --omit=dev`：0 high／critical，2 moderate，來自 ExcelJS 4.4.0 的 transitive uuid 8.3.2；目前 ExcelJS 無相容的較新版可直接解決，而且系統未使用該風險 API。
- 包含開發工具鏈的 `npm ci` 曾列出 25 vulnerabilities（1 low、8 moderate、16 high），多位於 dev dependency chain。不得把 production audit 的結果誤報成整棵依賴樹零漏洞。
- 禁止為消除數字而使用可能破壞 ExcelJS 的強制 downgrade／override；升級前須重跑 Excel 匯入／匯出與完整 E2E。

### 手冊驗證限制

- v20.63 PDF 手冊共 26 頁，已逐頁視覺檢查。
- DOCX 與 PDF 由同一 HTML 來源產生，但當次環境沒有 LibreOffice，因此 DOCX 沒有獨立渲染成圖片做視覺驗證。這是已知驗證限制，不得宣稱 DOCX 已完整視覺通過。

## 12. 發布、GitHub Pages 與交付規則

### 發布前

1. 實際核對 `pwd`、`git remote -v`、branch、HEAD、版本，避免選錯三套程式。
2. 候選包放在獨立乾淨工作目錄，不沿用舊版未追蹤產物或 node_modules。
3. 先讀 Claude 說明、SHA-256，再獨立判風險；SHA 清單只證明收到的包，GPT 若修檔就要產生新清單。
4. 確認只使用 npm，無 `pnpm-lock.yaml`／`pnpm-workspace.yaml`。
5. 執行風險等級要求的資料流檢查及全部現有 automated／regression／E2E／TS／lint／production／Pages build。
6. 核對 `app/system-release.ts`、`package.json`、lockfile、更新說明、手冊名稱、驗證檔、HTML meta 與畫面版號。
7. 用新建置輸出同步 Repository 根目錄的 `index.html`、`assets/`、`.nojekyll`、手冊及必要檔案，刪除已失效的舊 hash 資產。
8. 檢查 Git diff，禁止把往來說明、私人樣本、node_modules、測試輸出或機密上傳。

### 發布後

1. commit 並 push `main` 到 `https://github.com/BaoToast/DailyTraffic.git`。
2. 確認 remote commit 等於預期 commit。
3. 等待 GitHub Actions 測試成功及 Pages deployment 成功；不能只因 push 成功就宣稱網站完成。
4. 驗證 `https://baotoast.github.io/DailyTraffic/`，並用 `?v=<version-or-commit>`、帶版號手冊檔名和新版本唯一資產 hash 查線上內容。
5. 固定路徑如 `package.json`、`app.js` 或未帶版本 query 的快取結果不能作為「線上不是最新版」證據。
6. 驗證舊版專屬 URL 應失效或不再被首頁引用，避免新舊資產混放。

### 備份交付

- 使用者的固定交付位置：`D:\Users\95108\Downloads\交通系統交付_YYYYMMDD\全日交通量_<版本>\`。
- 同日同版若再次修正，不覆蓋既有交付，使用 `_第2次`、`_第3次`。
- 不自動刪除以前的交付檔。
- 交付至少包括：最新完整程式 ZIP、GitHub Pages 更新包、驗證／測試說明、SHA-256、給 Claude 的二次複查交接。
- 完整程式備份宜由已提交 commit 的 `git archive` 產生，以避免本機換行或未追蹤檔污染；必要建置檔（含 `.openai/hosting.json`）不能被排除。
- 給 Claude 的文件不得上傳正式 Repository，除非它本身是正式公開文件且使用者明確要求。

## 13. 已知問題、待確認與暫緩事項

### 已知但尚未處理

- `README.md` 是通用 starter 說明，容易誤導；實際維護先信本文件和程式碼。是否另行重寫 README 待使用者未來指示。
- ExcelJS transitive uuid 的 2 項 moderate production audit 尚無相容上游修正；持續監控，不做破壞性強制替換。
- DOCX 未在具 LibreOffice 的環境獨立視覺渲染；若未來手冊樣式改動，應在可用環境補做。
- CSV 能力沒有和兩類真實 Excel 樣本等量的完整證據，新增／變更時需補測。

### 待確認

- 無法只從 Repository 證明所有使用者現場歷史 Excel 變體都已涵蓋。Parser 修改若涉及新格式，應由使用者提供去識別樣本，並誠實記錄真實檔測試是否可執行。
- 其他兩套交通程式的目前本機路徑、版本及 commit 不屬本文件範圍，也未在本次交接核對。

### 已決定暫緩

- 三套系統合併成「交通調查智慧分析平台」。理由：三套系統仍需各自完成與驗證，現在整併會增加回歸與維護風險。
- 雲端多人版、即時跨裝置同步、中央帳號權限。理由：先完成本機版正確性與穩定性，之後再評估。
- 重建 GPT Site。理由：正式站已改為 GitHub Pages，舊 GPT Site 已刪除。

## 14. 使用者的長期開發與禁止事項

1. 以事實、證據和正確性優先；使用者假設與證據衝突時先重新驗證，仍不符就明確指出。
2. 資訊不足或不能確認時寫「待確認」，不得猜測或編造。
3. 使用者說「複查」等同執行第 15 節完整風險導向流程，不能因對話變長自行簡化。
4. 收到更新包時以最新、明確版號和 SHA 為候選基準，與舊候選隔離；不得沿用舊資產 hash。
5. 不因 Claude 說已測試而省略 GPT 獨立驗證。
6. 授權「有 bug 直接修正並發布」時，可在相同需求範圍內直接修正、測試、發布；不得擴張成未要求的重構。
7. 不改變已確認的交通工程公式、資料邊界和平假日語意，除非使用者明確批准新規格。
8. 現有工作已完成的 gate 不重做；先確認進度，從中斷點繼續。若新檔取代舊檔，才以新候選重新執行必要 gate。
9. 所有實際測試要回報精確 pass／fail／skip；未取得真實檔或無法執行的測試要明說。
10. GitHub Pages 是正式目標；CI 是測試，不是部署；GPT Site 不存在時不要回報它未更新。
11. 發布後提供完整備份與 Claude 二次複查文件；不覆蓋或刪除舊交付。
12. 本次與未來交接只更新同一個 `PROJECT_HANDOFF.md`，由 Git history 保存歷史。

## 15. 完整風險導向複查規則

這是本專案長期固定規則。未來使用者說「複查」、「請協助複查本次 Claude 提供的檔案」或「依照風險導向複查規則複查並發布」，除非另有指定，都代表以下完整流程。

### 低風險

針對本次修改、直接影響範圍及必要的間接影響進行增量複查。

### 中風險

複查所有受影響模組，並追蹤完整相關資料流及跨檔案依賴。

### 高風險

進行全專案層級複查。核心交通工程公式、共用資料結構、Parser／匯入架構、核心計算流程、大型重構、重大跨模組修改，以及可能廣泛影響既有結果的共用邏輯，原則上列為高風險。

### 所有風險等級共同要求

1. GPT 不受 Claude 宣稱的修改範圍限制。
2. 必須先完整理解 Claude 本次修改目的與修改說明。
3. 優先檢查本次修改、直接影響、間接影響及跨檔案依賴。
4. 發現可疑邏輯、既有 Bug、Regression、edge case 或其他非預期影響時，主動擴大複查範圍。
5. Claude 的「已修正／已測試／正常」只能作為資訊，不能作為驗證證據；GPT 必須獨立確認。
6. 必須驗證必要的完整資料流：輸入 → Parser → 驗證 → 資料結構 → 計算 → UI → 匯出。
7. 執行專案具備的完整 automated tests、regression、E2E、TypeScript、lint、production build 及 Pages build。
8. 涉及交通工程計算時，用可追溯案例核對顯示和 Excel 匯出；路段與路口、平日與假日、方向／流向及動態車種都按影響範圍回歸。
9. 發現缺陷時，在使用者已授權修正的範圍內直接修正。
10. 修正後重新執行相關測試及必要的完整 Regression。
11. 所有必要驗證通過後才能發布。
12. 發布後驗證 push、Actions、Pages deployment、線上新版唯一資產與帶版號路徑。
13. 提供 Claude 第二次複查所需的完整交接：Claude 原始修改、GPT 發現問題、Root cause、GPT 額外修正、測試／Build、正式版本、commit、發布證據及高風險項目。

不得因更換 GPT 對話、上下文變長、額度中斷或 Claude 已先測試而縮減本規則。若記憶不完整、規則衝突或無法確認「複查」的意思，先詢問使用者。

## 16. Claude ↔ GPT 固定開發流程

```text
Claude 修改／修復
  -> Claude 提供完整檔案、SHA-256 與修改說明
  -> GPT 確認正確程式、候選版號、Repo、branch、基準 commit
  -> GPT 獨立判定風險並依第 15 節複查
  -> GPT 發現問題時直接修正並記錄 root cause
  -> 執行必要且完整的 Regression／Test／TypeScript／lint／Build／E2E
  -> 全部必要驗證通過後發布 GitHub／GitHub Pages
  -> 驗證 Actions、Pages、線上資產和版本路徑
  -> GPT 提供完整程式、Pages 包、SHA、測試與 Claude 交接說明
  -> Claude Opus High 第二次獨立複查
```

Claude 的範圍描述不是 reviewer 的界線。若 GPT 修改了 Claude 包，原始 SHA 只用於證明收到的候選，正式交付必須重新產生 SHA。Claude 二次複查文件至少要列：原始修改、GPT 缺陷、root cause、實際修正、測試結果、版本、commit、發布網址／資產及仍需特別檢查的高風險點。

## 17. AI 對話換代交接規則

### 舊 GPT

1. 依本文件、當代有長期價值的對話歷史及**目前實際 Repository**更新同一份 `PROJECT_HANDOFF.md`。
2. 刪除或修正過時內容，補入新技術決策、公式、Bug、Regression、功能、測試、版本、部署、已知問題與待辦。
3. 假設下一個 GPT 完全看不到舊對話，做交接完整性自我驗收。
4. 實際核對程式資料夾完整路徑、remote、branch、HEAD、版本和部署，不照抄舊路徑。
5. 只提交交接文件及本次必要的文件變更，檢查 diff，commit 並 push。
6. 最後回報核對過的完整路徑、Repo、Pages、版本、branch、commit、檢查結果、待確認與可能因舊對話過長而不能可靠確認的資訊。

### 新 GPT

1. 先用使用者提供的資料夾核對 Repository 身分，不以聊天標題猜程式。
2. 完整閱讀 `PROJECT_HANDOFF.md`，再核對實際程式碼、Git、測試、Build、CI 及部署狀態。
3. 主動列出文件與現況差異，完成正式交接驗收。
4. **未經使用者明確確認「交接確認完成」以前，不得修改程式、commit、push、發布或開始待辦。**只可做必要的唯讀核對。
5. 未來只更新同一份 `PROJECT_HANDOFF.md`；不得建立 `PROJECT_HANDOFF_2.md`、`PROJECT_HANDOFF_3.md` 等，歷史由 Git 保存。

## 18. 事實來源與證據規則

當舊聊天、本文件、README、其他文件、實際程式碼、Git 與測試彼此衝突時：

1. 主動指出差異。
2. 以目前可驗證的 Repository、程式碼、Git 狀態及可重現測試為最終事實來源。
3. 版本應讀正式版本來源並用 Git／帶版號線上路徑交叉驗證，不信瀏覽器快取。
4. 無法確認者標示「待確認」。
5. 不得把推測、理論上應可、Claude 宣稱或未跑的測試寫成事實。
6. 真實附件因權限／路徑不存在而沒測時，要明列「未執行」，不能用合成 fixture 冒充真實檔通過。

## 19. 交接完整性自我驗收

本文件已按「新 GPT 完全看不到舊對話」檢查，涵蓋：

- 唯一正確的 DailyTraffic 路徑、Repository、origin、Pages、branch、版本與交接前正式 commit。
- 兩種調查格式、動態車種、路口幾何、PCU、尖峰、平假日、部分日、缺季、精度與資料隔離。
- 輸入、解析、驗證、儲存、計算、UI、匯出及備份的完整資料流。
- IndexedDB 競態、重複匯入、身分、歷史重大 regression 與禁止復發事項。
- 測試命令、v20.63 實際數量、未驗證限制、安全 audit 與部署證據。
- 完整風險導向規則、Claude／GPT 分工、Pages 發布、備份及下一代對話 gate。

### 新 GPT 接手後最合理的下一步

只做唯讀驗收：確認目前路徑仍是本文件第 1 節所列 DailyTraffic、`origin` 仍為 `BaoToast/DailyTraffic`、branch／HEAD／正式版本與 Pages 是否一致，完整閱讀本文件後向使用者回報差異。**完成回報後等待使用者明確說「交接確認完成」；在此以前停止，不得開始新功能、修正、commit、push 或發布。**
