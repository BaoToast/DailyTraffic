# 全日交通量及車種組成 v20.35 驗證報告

日期：2026-08-30
比較基準：v20.33（GitHub commit `ec10d6a5a9a29bfe7129c5d96310f5bebb02db53`）

## 一、本版範圍

v20.35 新增「調查日期 × 期別」一致性檢查與「季別 ⇄ 實際調查月份」顯示切換。原本規劃為 v20.34 的內容從未發布，已全部併入 v20.35。

本版沒有改變車輛數四捨五入、車種分類、PCU 當量、尖峰判定、方向比較、資料覆蓋鍵值或 Excel 匯出計算口徑。

## 二、日期檢查規則

- 日期落在所選期別內：直接匯入，不增加確認視窗。
- 日期與所選期別不一致：檢核報告紅底提示，寫明檔案、工作表、儲存格、原文、日期及所屬期別；寫入前再二次確認。
- 無法辨別日期：只提醒使用者自行確認，不顯示期別不一致、不跳二次確認且不阻擋匯入。
- 每一張真正匯入的工作表獨立檢查；同一檔案中某張有日期、另一張無日期時，仍會明確提醒缺日期的工作表。
- 有效曆日檢查含閏年；`115年2月29日`、`115年4月31日` 視為無效，`113年2月29日` 可正確辨識。
- 「製表日期」「列印日期」「報告日期」等非調查日期會被排除。

## 三、與計算和覆蓋的隔離

- `headerDateCells()` 只讀取表頭前 12 列的非空儲存格，沒有進入 `parseTrafficSheetValues()` 的數值解析路徑。
- `surveyDate` 是可選的唯讀記錄欄位，只供日期提示與月份顯示。
- `trafficIdentity()` 仍只由計畫、季別、調查點、日別、方向與時段組成，不含 `surveyDate`。
- 期別顯示切換只改畫面文字，資料分組、排序、計算與匯出仍以 `quarter` 為準。

## 四、GPT 複查時修正的交付問題

1. 修正同一檔案中「一張工作表有日期、另一張沒有日期」時，無日期工作表漏掉提醒的問題。
2. 補齊 `TrafficRecord.surveyDate` 型別定義，避免嚴格 TypeScript 檢查出現新增錯誤。
3. 新增發布守門測試，確保 GitHub Pages 根目錄的建置產物真正含目前版號與新功能，不會再把舊版網站資產當成新版上傳。
4. 將誤留的 `VALIDATION_v20.34.md` 更正為本檔 `VALIDATION_v20.35.md`，並更新為本次實際複查內容。
5. 交付包內同名 PDF 手冊仍是較舊內容，缺少本版日期檢查說明；已由 v20.35 Word 手冊重新產生 PDF 並完成逐頁版面檢查。
6. 更新說明最末的手動發布指引仍列 v20.33 版號與舊資產檔名；已改成 v20.35 實際資產與驗收路徑。

## 五、最終驗證結果

- `npm ci`：完成，使用鎖定版本安裝 587 個套件。
- `npm test`：217/217 通過；包含 lint、正式建置、日期、解析、計算、匯出、備份、權限、發布資訊與回歸測試。
- `npm run lint`：0 error、7 warning；7 項均為既有 React hooks 相依提示，未新增 lint 錯誤。
- `npm run e2e`：116 項瀏覽器檢查通過、0 失敗；涵蓋日期一致、不一致、無法辨別、部分工作表缺日期、期別顯示切換、匯入、分析、匯出、版面與跨計畫隔離。
- GitHub Pages 正式建置：成功；主程式 `assets/index-DfUqzNLG.js`，樣式 `assets/index-DwYFeMjP.css`。
- 根目錄與 `github-pages/dist`：入口、五項資產及 v20.35 DOCX/PDF 手冊 SHA-256 逐一相同。
- 手冊：Word 轉檔為 27 頁 PDF；DOCX 與 PDF 逐頁檢視未見文字重疊、截斷或缺字，且包含 v20.35 日期檢查說明。
- TypeScript 額外檢查：本版新增的 `surveyDate` 型別錯誤已消除；專案仍有既存的 Cloudflare worker 型別與 `.ts` 匯入設定錯誤，因此 `tsc --noEmit` 不是目前 CI 守門項目。
- `npm audit --omit=dev`：4 項（moderate 2、high 2、critical 0）；依維護規則僅記錄，未在本版強制升級相依套件。

結論：本版日期檢查、既有計算與發布內容均通過驗證，可發布為 v20.35。
