# 全日交通量及車種組成 v20.42 驗證報告

- 驗證日期：2026-09-04
- 發布目標：`BaoToast/DailyTraffic` 的 GitHub Pages
- 前一正式版：v20.40（commit `18f54d00c9013f37fdf49b742d45f58d44b84cab`）

> **關於版號：** v20.41 已產生但**從未發布**，內容全部併入本版，因此前一正式版是 v20.40。

## 本版性質

v20.42 的功能修正是季度輸入的**判定方式**與三支系統之間的**守門方式**；
GPT 複查時另修正輸入框限制、更新 SheetJS，並補齊相關驗證工具。
**交通量、PCU、尖峰、車種組成與方向分組的計算公式均未變更**，
可以填的年份範圍也完全沒有改（民國 90～200、西元 2001～2111）。

11 路段黃金值仍為全日實際交通量 `688,205 輛／日`、24 小時 PCU `530,122 PCU／日`。

## 本版修正

### 1. 季度輸入的判定改看年份數值，不看位數

v20.40 依「位數」分派：四碼一律當成西元年。於是把民國 115 打成 `0115Q1`
會被判成「西元 115 年、超出範圍」，回一句「年份超出可換算範圍：民國年請填
90～200」——**但 0115 就是 115，訊息與事實矛盾**。

而三支共用的 `normalizeSurveyPeriod()` 一直都把 `0115Q1` 算成 `115Q1`，
等於**同一個模組裡，檢查與正規化對同一個字串有兩種看法**。

改成以年份的**數值**判定（`Number()` 會吃掉前導零）：

- 民國 90～200 → 直接使用
- 西元 2001～2111 → 減 1911 換成民國年
- 其餘 → `range`

民國 90～200 與西元 2001～2111 兩段不重疊，數值本身就足以分辨，不需要位數。

同時補上 `NFKC` 正規化與去除所有空白：從 Excel／Word／PDF 複製貼上很常帶進
全形數字或全形 Ｑ，手打時也常多按一個空白。`１１５Ｑ１`、`115 Q1`、`2026 Q1`
現在都認得。

**放行時的儲存值一律與 `normalizeSurveyPeriod()` 的結果相同**——這一點由
契約逐筆驗證，避免同一季出現兩種寫法。

### 2. 三支系統的季度把關改由同一份「行為契約」鎖住

v20.40 那一輪，三支的 `checkSurveyPeriodInput()` 各自被改成不同寫法，而三支
的守門測試都只驗自己那一份，所以**沒有任何一支測試看得到分歧**。實測 67 種
輸入，三支有 12 種結果不同。

更糟的是路口轉向那一支寫成
`assert.match(here, /rocYear >= 90 && rocYear <= 200/)`——它斷言的是**某一種
實作的原始碼長相**，不是行為。結果是：想把三支同步回同一份程式，反而會踩紅
那支測試。守門測試從「防止分歧」變成「鎖住分歧」。

改法：

- 三支各放一份**逐位元相同**的 `tests/period-input-contract.mjs`
  （交通服務水準因為是扁平結構，放在根目錄）。
- 契約檔的 SHA-256 `638f2b48…45fe75` 由三支各自釘住。
- 每一支都把自己的實作跑過契約裡的全部 54 筆案例。
- 全程不跨包引用檔案，解壓後每一包都能獨立執行。

任何一支改了實作 → 它自己的測試紅；任何一支改了契約 → 那一支的雜湊對不上而紅。

### 3. 新手手冊大幅精簡

手冊裡累積了 29 則歷次版本的改善紀錄，**占整本手冊的一半以上（52%）**——
新手要看的教學被埋在裡面。而同一份歷史在 `CHANGELOG`、
「【更新說明】請先讀我.txt」與各版 `VALIDATION_*.md` 裡本來就有，
手冊那份是第四份複本；重複的歷史正是先前 CHANGELOG 漏掉 21 個版本那一類問題的來源。

使用者定的規則是：**手冊只寫「目前的功能是什麼、怎麼用」**——
「程式曾經出了什麼問題，修復方法是什麼對使用者來說不重要，那是程式維護者才需要知道的事情。」

所以本版把手冊裡的版本紀錄**全部移除（剩 0 則）**，並在該處指向 `CHANGELOG.md` 與更新說明；
同時把混在教學段落裡的**維護敘事改寫成現況描述**，例如
「過去這幾列顯示的是舊值……本版已修正」改成「這幾列直接顯示外面目前的數值」。

**教學內容一個字都沒有刪**，只拿掉歷史紀錄與維護敘事。

- PDF：**43 頁 → 24 頁（−44%）**
- 守門測試 `tests/manual-version-log.test.mjs` 三支斷言：版本說明 0 則、章節標題不掛版號、
  內文不得出現維護敘事。封面戳記的版號檢查改由 `release-metadata.test.mjs` 負責

### 本輪自己抓到的一個缺陷：根目錄的手冊沒有跟著更新

`build-pdf.mjs`／`build-docx.mjs` 只寫 `public/`，而實際發布的是倉庫根目錄那一份
（靠另外複製）。重建手冊時只更新了 `public/`，根目錄那一份還是舊手冊——頁數停在
清理前的數字，內容裡還留著已刪掉的維護敘事。**網站會讓使用者下載到舊手冊。**

原有測試全部通過：它們只檢查「檔案存在」「檔名帶本版版號」「舊版號檔案已刪除」，
根目錄那一份每一項都符合，只是內容是舊的。**「檔名對」不等於「內容對」。**

已把 `public/` 的產出複製到包裡其他每一個位置，並新增
`manual-copies-identical.test.mjs`：掃出包裡所有本版檔名的手冊逐一比對 SHA-256，
出現兩種內容就紅字並指名是哪一份。**突變測試：改掉根目錄那一份一個位元組 → 立即紅字。**

## 驗證結果

- ESLint：0 錯誤。
- `npm run build`：成功。
- 單元／回歸測試：**279 項全部通過、0 失敗、0 略過**（含 GPT 複查新增 4 項）。
- `npm run e2e`：**155 項檢查全部通過，無 JavaScript 例外**。
- 突變測試：把實作改回 v20.40 的位數判定，`季度輸入把關必須完全符合三支共用的行為契約` 立即紅字。

## 真實調查檔回歸

以 37 份真實調查工作簿逐檔比對 v20.42 與 v20.40 的解析結果：

- 解析列數：**2,256 列**
- 逐檔逐欄比對：**37/37 逐字相同**

**本版沒有讓任何一個數字改變。**

## 跨系統相互驗算

- 同一份檔案由本系統與路口轉向分別解析，逐車種車輛數（含自訂車種）
  **12/12 完全相符**。
- 三支對同一批工作簿的調查日期判讀：**186/186 個工作表完全一致**。
- 三支共用期別模組的 `normalizeSurveyPeriod()`、`checkSurveyPeriodInput()`、
  `surveyPeriodInputMessage()`、`quarterInYearStyle()`：**7/7 組探測輸入完全一致**。

## 建置產物與手冊

- 根目錄網站資產已用本版重建：主程式 `assets/index-CG6scn_7.js`
  （SHA-256 `d6eec0b782170a51a0308ab75bbc46244587af6d591181b47ef125ca5cdbb7f1`）。
- PDF 手冊 24 頁、Word 手冊 133 區塊，均已重新產生；改寫過的段落所在頁已逐頁轉圖檢查，
  未見截斷、重疊或破版。

## GPT 複查追加修正

- SheetJS 已由 npm registry 的 0.18.5 更新為官方 0.20.3，並將官方 tarball
  收錄於 `vendor/xlsx-0.20.3.tgz`，避免安裝時依賴 SheetJS CDN。
- 新增雜湊與實際安裝版本守門測試；tarball SHA-256 為
  `8dc73fc3b00203e72d176e85b50938627c7b086e607c682e8d3c22c02bb99fe8`。
- 移除季度輸入框舊的瀏覽器 `pattern` 限制，避免它在共用檢查函式之前攔下
  `１１５Ｑ１`、`115 Q1` 等契約明確接受的輸入，並新增 UI 回歸測試。
- SheetJS 升級不變更任何交通量計算公式；既有匯入前後原型污染檢查仍保留為
  額外防護。

## 計算界線

本版沒有變更：交通量、PCU、尖峰挑選、車種組成、路段方向、路口駛入／駛出的
任何公式；也沒有變更可接受的年份範圍。
