# 全日交通量及車種組成 v20.49 驗證報告

- 驗證日期：2026-09-05
- 前一正式版：v20.47（GPT 發布，commit `3e6a3bd5d9882c20a2cc31dc3a5b918956f11880`）
- 本版性質：**修正畫面提示、重畫時機、單位文字與儲存層去重實作；交通量與 PCU 計算規則沒有變更。**

## 本版修正

### 只剩路段時，「路口流量視角」沒有作用，但畫面沒說

使用者回報：在「時段車種分析」把調查點篩選縮到只剩一條路段之後，
「駛出路口／駛入路口／駛出＋駛入並列」三種視角切下去數字完全一樣，
懷疑篩選壞掉。

**先確認：數字一樣是正確的。**「駛入／駛出」只對路口有意義
（車從哪條支線進、從哪條支線出），路段只有方向A／方向B。
`computePeriodRows()` 本來就有對應的過濾
（`(row) => firstMode || row.surveyType === "intersection"`），
並列時若不濾掉，同一筆路段會被輸出兩次。

實測佐證（同一批樣本，中山路＝路段、中正路口＝路口）：

| 支線 | 駛出視角 | 駛入視角 |
|---|---|---|
| 路口A | 1,695 | 1,327 |
| 路口B | 1,501 | 1,327 |
| 路口C | 1,318 | 1,551 |
| 路口D | 1,190 | 1,499 |
| **全部支線合計** | **5,704** | **5,704** |

路段三種視角完全相同；路口各支線換視角就不同、合計相同——
同一批車換個角度看，分配不同但總量不變。功能是對的。

**要修的是畫面沒有講這件事。** 匯出中心本來就有一行
「只有路口格式的調查點會受這一項影響；路段格式一律是方向 A／方向 B。」，
但這一區沒有，兩邊不一致。

本版：結果只剩路段時停用該下拉，並顯示
「本次結果只有路段，駛入／駛出僅適用於路口，所以這一項暫時停用。」
篩選放寬後自動恢復，且**刻意不重設**使用者原本挑的視角。


### 匯入時看不到「讀取中」（兩段空窗期）

使用者回報：把檔案匯入時畫面沒有任何提示，一直到匯入完成才出現
「已選取 X 份檔案」；按「讀取並預覽」時反而有正常的進度條。

實測診斷（不是猜測）：在頁面裡掛一支 20ms 的 `setInterval` 心跳，
用一份合成的 1.7MB／24,593 列檔案匯入，心跳在整段解析期間只跳了一次。
主執行緒被 SheetJS 解析完全佔滿，React 沒有機會重畫。這一段是
「程式已經收到檔案、但畫不出來」。

再往前一段更難處理：按下「選擇檔案」到瀏覽器把檔案交給程式之間，
是瀏覽器在讀檔，程式收不到任何事件，連 `change` 都還沒發生。所以
提示只能從**按下按鈕那一刻**開始顯示，不可能等到程式拿到檔案才顯示。

本版兩段都補：

1. `onClick` 當下 `setPickingFiles(true)`，畫面立刻出現
   「正在讀取您選擇的檔案，請稍候…」；`onChange` 收掉。
2. 按「取消」時瀏覽器不發任何事件，改以 `window` 重新取得 focus
   後 1.2 秒當退路收掉（延遲是為了不把正常選檔誤判成取消——
   正常選檔的 `change` 會先到）。
3. 第一份檔案開始解析前，先 `await paint()`——連續兩個
   `requestAnimationFrame` 保證真的重畫過一次。`setTimeout(0)` 只保證
   讓出一個巨集任務，**不保證重畫**，所以不能用。

`paint()` 附 250ms 時間退路：分頁在背景時 `requestAnimationFrame`
完全不觸發，少了退路匯入會永遠停住。

只有第一份檔案前面加這一次等待，後續各份維持原本的 `breathe()`，
不額外拖慢匯入。



## v20.49：全面徹查後的一項修正

### 畫面與匯出對同一格標不同的單位

畫面走 `metricUnitFor(metric, period, { partial: surveyScope.partial })`——
那個 `partial` 是**整批的代表值**（任何一個調查點是部分時段，整批就算部分
時段）；而匯出與報告草稿走 `cellUnitFor`，看的是**每一列自己的時段標籤**。
同一個數字，畫面與 Excel 說兩種話。

`cellUnitFor` 的註解本來就寫著「不能看整批的 partial 旗標」，但畫面那一側
從來沒有改過來。舊的守門測試 `cell-unit.test.mjs` 只驗這支輔助函式、
**沒有驗任何一個消費端**，所以真正還在用整批旗標的畫面路徑完全沒被守住。

實測 6 種情境有 3 種不一致：

| 情境 | 畫面 | 匯出／草稿 |
|---|---|---|
| 完整 24 小時的調查點，但畫面上另有部分時段調查點 | 輛/調查時段 | **輛/日** |
| 2 小時一格的尖峰視窗 | 輛/hr | **輛/該時段（120 分鐘）** |
| 湊不滿一小時的尖峰視窗 | 輛/hr | **輛/該時段（45 分鐘）** |

**第一種現在就會發生**：真實資料裡同時有 24 小時（11017T1506、T1507）與
4 小時（11017T1501）的調查點，只要落在同一個畫面範圍，做滿 24 小時的
調查點就會被標成「輛/調查時段」。

修法：把匯出那邊本來就正確的欄位單位邏輯抽成 `columnUnitFor()`，
畫面四處與匯出**共用同一支**——不是各自再寫一份規則，而是由結構保證一致。
整欄時段一樣就用該時段的正確單位；長短不一時明講「見『分析時段』欄」，
不挑一個代表值假裝整欄都適用。

**只動單位字串，沒有動任何數值。** `period-analysis.ts` 因抽出共用的
`columnUnitFor()` 而有程式碼差異；其數值計算路徑沒有變更。其餘七個解析與
計算模組相對 v20.47 逐位元相同（以 Git blob 逐檔比對驗證）。

守門測試改為驗「同一組時段標籤，畫面與匯出算出來的欄位單位必須相同」。
突變測試：混合時段時改成挑第一個代表值 → ❌ 紅字。

## 使用者追問的重複匯入防護（本輪加驗）

使用者的顧慮（原話）：「我怕如果使用者誤以為沒匯入到資料，然後重複匯入相同資料，
系統卻沒有阻止，這樣才是異常。」

新增 `scripts/e2e-reimport.mjs`，實際在瀏覽器裡把同一批檔案匯兩次：

| 檢查 | 實測結果 |
|---|---|
| 匯入期間有處理中遮罩 | ✅ |
| 遮罩**真的擋住**底下的操作（用 `elementFromPoint` 驗，不是只看有沒有那個元素） | ✅ 26/26 次 |
| 第一次的檢核報告 | ✅ 新增 288 筆、覆蓋 0 筆 |
| 第二次的檢核報告（**寫入前**就看得到） | ✅ 新增 0 筆、**覆蓋 288 筆** |
| 同一批檔案匯第二次，紀錄數不翻倍 | ✅ 288 筆 → 288 筆 |
| 主鍵唯一 | ✅ 288 筆／288 個相異 `_id` |
| 匯入中重新整理頁面 | ✅ 0 筆，沒有留下半筆資料 |

突變測試：

| 對程式做的破壞 | 結果 |
|---|---|
| `recordIdentity()` 每次回傳不同的鍵（等於完全不去重） | ❌ 紅字：288 筆 → **576 筆** |
| 修正後：把共用的 `trafficIdentity()` 弄壞 | ❌ 2 項紅字（資料翻倍＋報告同步改變） |

### 由這次突變測試發現的一個隱患——**本版已修**

去重的鍵原本在**兩個檔案各寫了一份**，欄位完全相同：

- `app/final-workflow.ts:133` 的 `trafficIdentity()` → 決定**檢核報告寫給使用者看的**「新增 N／覆蓋 N」
- `app/app-fetch.ts:11` 的 `recordIdentity()` → 決定**儲存層實際怎麼做**

兩支當時一字不差、行為正確，但突變測試時出現了這個畫面：把 `recordIdentity()`
弄壞之後，**檢核報告仍然顯示「覆蓋 288 筆」，實際卻寫進了 576 筆**。
也就是使用者會看到一份寫著「已覆蓋」的報告，而資料其實重複了——
每一筆都合法，總量守恆的檢查抓不到。

本版把 `recordIdentity()` 改成直接轉呼叫 `trafficIdentity()`，
讓「報告怎麼算」與「儲存怎麼做」永遠是同一份實作。

**沒有動到計算模組**：`app/final-workflow.ts` 相對 v20.47 仍**逐位元相同**
（`cmp` 驗證），只有 `app/app-fetch.ts` 這個離線儲存轉接層改了，
新增一行 import、把六個欄位的複製實作換成一次轉呼叫。

修正前後的突變測試對照：

| | 拿掉去重之後的結果 |
|---|---|
| 修正前（兩份實作） | ❌ 只有 1 項紅字：資料 288→576，**但報告仍寫「覆蓋 288 筆」——報告說謊** |
| 修正後（單一實作） | ❌ 2 項紅字：資料 288→576，**報告同步變成「新增 288 筆、覆蓋 0 筆」——報告不再說謊** |

修正後報告與儲存一起動，這正是要的效果：兩者不可能再各說各話。

### 這一支測試自己踩過的坑（已修，記錄下來避免再犯）

用 `button:has-text("取消")` 當萬用關閉鍵時，檢核報告上的
**「取消，資料不變」會先被選中**，等於每一次都把匯入取消掉，
IndexedDB 一直是 0 筆——而「不可以翻倍」那一項照樣全綠（0→0）。
是靠「第一次匯入之後確實有資料」這個前置檢查才抓到。
現在推進鍵一律優先挑肯定語意的那一顆，否定語意的絕不當關閉鍵。

## 計算邊界（本版最重要的一點）

相對 v20.47，整份專案只有這些檔案有差異：

- `app/DashboardClient.tsx`
- `app/app-fetch.ts`（離線儲存轉接層：去重鍵改為轉呼叫 `trafficIdentity()`）
- `app/globals.css`（新增提示樣式）
- `app/period-analysis.ts`（抽出畫面與匯出共用的單位文字函式；未改數值計算）
- `app/system-release.ts`（只有版號字串 v20.47 → v20.49）
- `scripts/manual/manual.html`（只有手冊封面版號戳記）
- `scripts/e2e-period.mjs`、`scripts/e2e-progress.mjs`、`scripts/e2e-reimport.mjs`（守門測試）
- `tests/cell-unit.test.mjs`（新增畫面與匯出單位一致性守門測試）

以上是 `app/`、`scripts/`、`tests/` 三個目錄逐檔比對後的**完整**清單，
不是舉例。

`DashboardClient.tsx` 的差異全部是唯讀或畫面狀態：

1. 新增 `periodHasIntersection`——一個 `useMemo`，只是**讀** `periodRows`
   判斷有沒有 `surveyType === "intersection"`，不參與任何運算
2. `<select>` 加上 `disabled={!periodHasIntersection}`
3. 新增一個 `<small>` 說明文字
4. 新增 `pickingFiles` 狀態、focus 退路 `useEffect`，與
   `.picking-files-hint` 提示元素——只影響畫面，不碰檔案內容
5. 第一份檔案前 `await paint()`——只是等一次重畫，
   解析函式、迴圈順序與傳入的檔案完全沒有變

`computePeriodRows()`、`buildPeriodRows()` 與 `periodRows` 一行都沒有動。

下列七個解析與計算模組相對 v20.47 **逐位元相同**（Git blob 逐檔比對）：
`traffic-parser.ts`、`vehicle-analysis.ts`、
`period-date.ts`、`conclusion.ts`、`report-draft.ts`、`final-workflow.ts`、
`intersection-flow.ts`。

`period-analysis.ts` 不是逐位元相同：本版新增並套用 `columnUnitFor()`，
用途只限依各列時段產生正確的欄位單位文字。混合時間格仍只顯示提醒，
沒有更改尖峰挑選、時段切分或任何交通量／PCU 數值計算。

固定計算黃金值：11 路段全日實際交通量 688,205 輛／日、
24 小時 PCU 530,122 PCU／日，與 v20.47 完全相同。

## 突變測試

守門測試加在 `scripts/e2e-period.mjs`——本檔同時匯入路段（中山路）與
路口（中正路口）兩種格式，兩種情境才都成立；只驗其中一種會變成
恆真的假檢查（第一版寫在 `e2e-multi-filter.mjs`，那一支只匯入路段，
「結果含路口時可以選」永遠測不到，已改放這裡）。

| 對程式做的破壞 | 結果 |
|---|---|
| 拿掉 `disabled`（回到 v20.47 行為） | ❌ 「只剩路段時要停用」紅字（disabled=false） |
| 說明文字不分情境、永遠顯示同一句 | ❌ 「要說清楚為什麼停用」紅字 |
| 拿掉 `.picking-files-hint` 元素 | ❌ 「按下選擇檔案之後馬上看得到提示」紅字 |
| 拿掉 focus 退路（取消時不收提示） | ❌ 「按了取消之後提示要自己收掉」紅字 |
| 拿掉 `paint()` 的 250ms 時間退路 | ❌ 「rAF 不觸發時匯入仍會完成」紅字（20 秒沒完成） |

守門測試同時釘住四件事：含路口時可選、只剩路段時停用、停用時說明文字
要換成「只有路段」那一句、以及篩選放寬後要恢復。

## 驗證結果

- ESLint：0 錯誤（6 個既有 Hook 警告，與 v20.47 相同）。
- 單元／回歸測試：282 項全部通過、0 失敗、0 略過。
- 端到端瀏覽器驗證：9 支腳本全部通過（本版新增 `e2e-reimport.mjs`）。
- 手冊：重新產生 v20.49 PDF 與 Word；`manuals/` 與 `public/manuals/` 副本雜湊一致。
- 正式依賴安全掃描（`npm audit --omit=dev`）：0 個高風險、0 個重大風險；
  另有 2 個既有中等風險，均源自 ExcelJS 間接使用的 uuid。npm 提供的
  自動修正會把 ExcelJS 降至 3.4.0，屬可能破壞相容性的主要版本變更，
  因此本輪不為了消除警告而降級匯出套件。

## 證據界線

本包尚未推送，因此沒有 GitHub Actions run 編號，也沒有線上網址的核對結果。
