# 全日交通量及車種組成 v20.39 驗證報告

- 驗證日期：2026-09-01
- 發布目標：`BaoToast/DailyTraffic` 的 GitHub Pages
- 前一正式版：v20.38（commit `4dcf4b4a511837b375e1b5ad5b4a01ba8f5f78ae`）

## 本版性質

本版是**三支系統（全日交通量、路口轉向、交通服務水準）一次跨系統徹查**的結果。方法是拿使用者提供的 **37 份真實調查表**（案號 11017／11535／14013，含 7 份舊版 `.xls` 與 2 份大寫 `.XLS`）分別餵給不同系統，比對彼此的判讀與數字，凡是不一致的就往下追。本系統修正九項。

**計算口徑完全沒有變動。** 11 路段基準仍為全日實際交通量 `688,205 輛／日`、24 小時 PCU `530,122 PCU／日`。

## 修正項目

| 代號 | 嚴重度 | 項目 |
|---|---|---|
| H2 | 高 | 「平日＋假日」匯出把兩天的加總標成單日值 |
| H3 | 高 | `.xls` 的「平假日比較」用程式屬性名當欄名、少兩欄、無單位 |
| M1 | 中 | 三支共用的非調查日期清單漂移（缺彙整／輸出／建檔／產製） |
| M4 | 中 | 沒做假日調查時顯示「假日 0 輛／日、−100%」 |
| M8 | 中 | 跨計畫比較的實際交通量單位寫死「輛/調查日」 |
| L1 | 低 | 同季平假日面板的單位寫死，與同面板的切換鈕矛盾 |
| L2 | 低 | 還原備份後 PCU 面板仍稱「本計畫尚未自行設定」 |
| L3 | 低 | 沒有路口資料時引導使用者去看一張不存在的工作表 |
| L4 | 低 | 兩個調查點同名時匯出的 SUMIFS 會重複計算 |

### H2 詳述

`sheetActualUnit` / `sheetPcuUnit` 只看 `surveyScope.partial`，沒有看 `dayType`。日別選「平日＋假日」時 `scoped` 會同時納入兩天的紀錄（`dayType === "平日＋假日" || r.dayType === dayType`），匯出的是加總。實測 `14013T601鳳北路平、假日.xlsx` 匯出 12,838 輛（平日 9,392＋假日 3,446）被標成「全日實際交通量（輛/日）」，比真正的平日量高 37%。畫面上的 `dailyActualUnit` 一直是兩個維度都看的，匯出這一支沒跟上；同一份匯出檔的「全日時段車種分析」又標成「平日＋假日全部時段」、「歷季趨勢」把兩天拆回來，三處互相矛盾。

本版抽出 `exportActualUnit()` / `exportPcuUnit()`，`.xls` 與 `.xlsx` 兩條匯出路徑共用，欄位名稱也改為「平假日合計實際交通量」「平假日合計PCU」。

### H3 詳述

`add("current", dayComparisons, "平假日比較")` 把 React 狀態物件直接交給 `XLSX.utils.json_to_sheet`，欄名即為程式碼裡的屬性名。實測產出標題列為 `["roadId","roadName","weekdayActual","holidayActual","weekdayPcu","holidayPcu"]`，無單位，且缺「平假日差」與「假日相較平日（%）」。其餘每一個 `add()` 傳的都是中文鍵物件，僅此一處例外。

### M4 詳述

`dayComparisons` 把 `holidayActual` 初始化為 0 之後不再區分「未調查」與「調查結果為 0」。`11017T1503台1路科一路口.xlsx` 只有平日工作表，畫面顯示「平日 13,488 輛／日 ／ 假日 0 輛／日 ／ -100.0%」。同一份檔案的「歷季趨勢」處理是對的（寫 null 並附註解說明不可用 0），平假日比較這條路徑漏掉。本版新增 `weekdaySurveyed` / `holidaySurveyed` 兩個旗標，畫面顯示「本季未調查」，匯出寫空白格。

## 追加：季度可以填民國年或西元年，畫面與匯出可以切換兩種寫法

| 代號 | 嚴重度 | 項目 |
|---|---|---|
| Y1 | 中 | 匯入的年度欄位只收民國年 |
| Y2 | 中 | 匯出的三張工作表中，路段明細與路口明細的季度欄漏接顯示換字（同一份 .xlsx 裡兩種年份寫法） |
| Y3 | 低 | 更新說明的「如何更新」段落自 v20.35 起未同步版號，列出的網站資產檔名是三版前的 |
| Y4 | 低 | 根目錄的網站建置產物（實際部署的那一份）未隨版本更新 |
| — | 新功能 | 頁首「年份顯示」切換鈕：民國年 ⇄ 西元年，畫面與匯出一起換 |

### 切換是純顯示——這一版最需要被證明的一件事

年份切換只換看到的字。分組、排序、識別鍵、季度篩選與所有計算一律走**儲存值**（民國年）。實作上的關鍵是**下拉選單一律 `value={q}` 儲存值、只換顯示文字**：結論草稿的單季條件是 `row.quarter === scope.quarter` 直接比字串，值一旦跟著換成西元年，畫面立刻變成「所選條件沒有對應的資料」。

### Y2 詳述（自己抓出來的）

第一輪只改了「歷季」那一張表，同一支匯出函式裡的「路段明細」與「路口明細」兩張表仍寫 `季度: quarter`。結果是同一個 `.xlsx` 裡三張工作表出現兩種年份寫法，而每一張表自己看起來都沒有問題。本版新增守門測試直接數 `季度: quarter,` 的出現次數必須為 0。

### Y4 詳述

這個 repository 的 GitHub Pages 是從根目錄發布的，`index.html` 與 `assets/` 就是線上的網站。原始碼、版號、手冊都更新了，但根目錄的建置產物仍是舊的——實測本版在複查之前，根目錄的 `assets/index-*.js` **不含年份顯示切換鈕**。已重新建置並同步，且新增守門測試比對「更新說明列出的網站資產檔名」與「根目錄 `assets/` 的實際內容」。

## GPT 複查追加修正

Claude 交付內容的原計算與九項修正均保留。複查另發現並修正兩個邊界問題：

1. **同季平假日的調查涵蓋可能不同。** 原交付版只用一個共用涵蓋標示；例如平日完整 24 小時、假日只調查 4 小時時，畫面與 Excel 仍會計算差值與百分比，容易把不可直接比較的資料誤讀成日別差異。現在平日、假日各自顯示調查涵蓋，只有兩邊的實際時段區塊完全相同時才計算差值與百分比；否則顯示「涵蓋不同」，Excel 留白。
2. **管理季度改名可輸入西元年，但原交付版會照原字串儲存。** 這與本版「一律以民國年儲存」的核心界線衝突，也可能讓同一季以 `115Q2`、`2026Q2` 形成兩筆。現在改名前先正規化為民國年，並以正規化後的值檢查重複季度。

兩項均補入單元守門及實際瀏覽器匯出 Excel 的端對端驗證；交通量、PCU、尖峰與車種組成的計算公式未變。

## 驗證結果

- TypeScript `tsc --noEmit`：0 錯誤。
- ESLint：0 錯誤，7 項既有 React Hook 建議警告（與 v20.38 相同，無退步）。
- 單元／回歸測試：**264/264 通過**（v20.38 為 245 項；Claude 原交付新增 18 項，GPT 複查再新增 1 項調查涵蓋守門測試）。
- **守門測試已證明對未修正的 v20.38 原始碼會失敗**：以本版的測試檔搭配 v20.38 的原始碼執行，跨系統守門與發布中繼資料兩支的新增項目**全部失敗**。
- **年份切換的守門測試已證明會抓到天真實作**：在本版原始碼上分別植入三種常見錯法——(a) 匯出表寫回 `季度: quarter`、(b) `<option>` 的 `key`／`value` 用顯示文字、(c) 結論草稿的季度分段標題漏接顯示換字——三次都被對應的測試抓到。
- 端對端：`npm run e2e` 全部通過，**154 項檢查、0 失敗**、瀏覽器主控台無 JavaScript 例外；除原有年份切換驗證外，另以實際瀏覽器匯出 Excel 驗證平日、假日各自的調查涵蓋、涵蓋不同時採中性單位且不計算差值與百分比。
- 計算黃金值：11 路段基準與 PCU 係數黃金值全部一致（24/24 通過）。
- **跨系統驗算**：37 份真實檔中，兩支系統都判定為轉向資料的 12 組，**逐車種完全相同**（v20.38 時總數相同但車種歸屬不同）；28 組路段檔由路口轉向正確拒收。
- 三支共用的 `period-date` 模組已同步，四種非調查日期在三支的判斷完全一致；本版再同步新增的 `quarterInYearStyle()`、`YEAR_STYLE_LABELS` 與 `periodDisplayLabel()` 的 `yearStyle` 參數。
- **真實檔數值回歸：37 份真實檔逐張工作表跑完整解析，輸出與已發布的 v20.38 逐字相同。** 本版沒有任何一個數字改變。

## 已知套件警示

`npm ci` 仍回報 23 項相依套件警示（1 low、6 moderate、16 high）。依既定維護規則未強制升級，避免破壞舊版 Excel 相容性。與本版計算無關。

## 結論

v20.39 修正兩項會影響匯出數值標示的高風險問題與七項中低風險問題，計算口徑未變更，回歸、黃金值與跨系統驗算全數通過，可發布至 GitHub Pages。
